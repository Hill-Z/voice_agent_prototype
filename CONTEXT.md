# 当前进度

## 2026-08-18 主题匹配上下文配置
- 单个机器人的「主题管理」页拆分为上方默认收起的「主题匹配设置」和下方原主题列表，原有增删改查、筛选、排序与分页保持不变。
- 新增「历史对话辅助匹配」开关和历史消息条数，机器人与用户消息统一按条计数；默认开启并读取最近 5 条，可配置 1–50 条。
- 新增「上一轮主题辅助匹配」开关，将上一轮匹配主题作为下一轮判断线索；旧配置默认关闭，避免自动改变已有机器人行为。
- 按页面反馈将展开内容压缩为两行配置，条数输入与开关同行，说明文案各保留一句。
- 已验证：构建和 2 项需求检查通过；本地页面完成默认收起、展开、开关联动、默认值和原主题列表检查。

## 2026-08-18 本地服务启动
- 已按 `package-lock.json` 安装前端依赖，并统一使用 npm 官方源完成下载。
- `npm run build` 已通过；本地 Vite 服务已启动在 `http://127.0.0.1:5173/`。
- 当前 Node.js 为 20.11.1，依赖提示建议升级到 20.19.0 或更高版本，但不影响本次页面启动。
- 项目开始使用 Git 管理，首次版本关联仓库 `Hill-Z/voice_agent_prototype`。

## 2026-08-04 机器人级多 Agent 编排
- 机器人配置新增「Agent 编排」页签，简单场景保留单 Agent，复杂场景支持 Supervisor 统一调度与 Handoff 直接接力。
- 多 Agent 使用现有 Flow、旧版意图流程和普通主题作为对话 Agent 来源；任务 Agent 通过任务提示词、多工具、结构化输出和超时配置静默执行，不接管 TTS。
- 编排画布支持节点拖动、缩放、平移、连线、节点/边删除，以及入口 Agent、兜底 Agent 和最大转交次数配置。
- Flow/主题 Agent 默认继承关联资源，也可覆盖工具和知识库；人工目标从机器人已有 IVR 配置汇总；挂机节点配置多条结束话术。
- Supervisor 连线表达能力分派，仅配置可选准入条件；Handoff 连线配置触发条件和优先级；委派、并行、转人工和兜底分别使用独立配置字段及校验规则。
- Supervisor 与 Handoff 各自保存独立画布草稿，切换模式不会删除或改写另一模式的节点和连线。
- 编排保存纳入机器人统一草稿与版本发布，不单独建立应用或独立发布体系；旧机器人默认保持单 Agent。
- 已验证：`npm run build` 通过；浏览器完成 Supervisor 准入条件、旧版流程资源、任务 Agent 配置和 Supervisor/Handoff 草稿隔离检查。

## 2026-08-03 自动跟进菜单与 Flow 管理
- 自动跟进调整为一级菜单，下设「跟进任务」「规则编排」「跟进记录」三个二级菜单，移除页面内页签切换。
- 跟进任务支持新建和编辑，并关联已发布的跟进 Flow；任务保存时记录 Flow 名称和版本。
- 规则编排先进入 Flow 列表，支持新建、编辑、复制、发布、停用和删除，再进入独立无限画布编辑；已关联任务的 Flow 不允许删除。
- 跟进记录按单次执行明细展示任务、客户、Flow 版本、执行次数、结果和结果说明。
- 手动完成和转人工会同步生成执行记录；历史任务始终展示创建时的 Flow 名称和版本。

## 2026-07-22 业务事件与业务漏斗
- 监控报表保留原有 Topic / Flow 统计，新增独立“业务漏斗”工作区，避免重复实现 Flow 路径分析。
- 业务事件支持平台事件、Tool 返回、接口回调、通话变量四类来源，Tool / 回调 / 变量使用结构化字段、条件和值定义；事件可新增、编辑、启停，已被漏斗使用的事件不可删除。
- 漏斗支持选择机器人和版本、添加/删除/上下移动阶段，每个阶段必须绑定已启用业务事件；统计使用通话事件严格按顺序计算，同一通话和阶段只计一次。
- 漏斗以宽度递减的真实漏斗展示到达量、上一步转化率、累计转化率和流失量；点击阶段或流失量可查看对应通话记录与流失原因。
- 当前为前端原型和 mock 数据，业务成功只通过 Tool 返回、接口回调或变量条件事件记录，不由模型推断。
- 已验证：`npm run build` 通过；本地页面已检查事件列表、配置弹窗、漏斗阶段和通话下钻。

## 2026-07-20 安全护栏、工具分级与异常策略
- 对话策略新增固定开启的协议护栏，配置敏感信息处理、业务禁答规则和多条安全兜底话术；流式解析器过滤隐藏、未知和未闭合标签，并将转人工/挂机标签拆为动作。
- 工具编辑新增默认执行方式和按参数判断的执行规则，支持自动执行、用户确认、人工审批、仅人工处理，并配置确认话术、人工队列、IVR和审批超时。
- 异常处理支持机器人全局兜底动作、IVR、连续异常上限和多条话术；Flow Step 可继承全局或覆盖为继续、跳转、转人工、挂机。
- 兼容 `#Transfer[ivr:<id>]` 与旧客户常用的 `#Transfer[<id>]`；工具规则只对声明为数字的参数做数值比较，字符串订单号保持精确匹配；短回复词和强制打断词支持保存为空；工具变量引用增加可选变量池和空引用校验。
- 新增静态检查与可执行行为测试，完成真实页面验证；原型截图保存在系统临时目录 `voice-agent-tb-new`。
- 已在 TB 创建并附截图：RD-3897（安全护栏）、RD-3898（工具调用分级与人工处理）、RD-3899（全局与 Step 异常处理策略）。

## 2026-07-20 流程与主题列表筛选
- 流程配置列表新增按名称或描述搜索，并支持清空及无结果状态。
- 主题管理新增流程/普通分类筛选和分类列，保留原有搜索、状态、排序和分页。

## 2026-07-22 监控报表漏斗与业务事件配置入口
- 监控报表拆分为「综合报表」「漏斗配置」「事件配置」三个二级菜单，原监控报表页面保留为综合报表。
- 新增漏斗配置页：展示漏斗名称、绑定机器人、版本、阶段、启用状态，并支持进入漏斗编辑面板调整阶段顺序、事件和启用状态。
- 新增业务事件配置页：展示事件名称、事件编码、来源、来源对象、条件、启用状态，并支持编辑事件定义。
- 综合报表中的业务漏斗保留真实漏斗图、阶段人数、转化率、流失人数和通话明细下钻，配置页与分析页职责分离。
- 已在 `artifacts/business-funnel-rd/` 保存综合报表、漏斗配置、漏斗编辑、事件配置、事件编辑 5 张原型截图。
- `npm run build` 已通过，Vite 仅提示现有 bundle 体积较大。

## 2026-07-22 报价与商品计价 MVP
- 报价与商品计价统一为「智能计价」工具类型，通过单次报价、连续计价、同时支持三种使用方式区分运行行为；保留旧 QUOTE/CART_PRICING 配置读取兼容。
- 智能计价工具保留原有「编辑」入口，用于维护工具名称、函数名称、描述、模型使用指南、参数、调用控制和运行控制；列表额外提供「配置计价」入口。
- 计价配置支持平台计价与客户接口两种来源。平台计价内部包含价格数据、计算规则、导入记录、计价测试四个页签；客户接口仅保留接口配置和计价测试。
- 价格数据支持价格表版本下拉、编码/名称/规格搜索、分类/区域或门店/状态筛选、20/50/100 条分页、Excel 导入、新增和编辑价格。
- 计算规则支持搜索、计价方式/状态筛选、分页及固定价、按量计价、基础价加增量、阶梯价结构化编辑。
- 导入记录支持文件名/操作人搜索、状态筛选、分页；导入结果支持逐行搜索、结果筛选、分页和失败数据下载。
- 计价测试分别提供单次报价试算与连续购物车计价，展示命中规则、计算明细和合计金额。
- 价格表支持维护自定义字段，字段可配置文本、数值、选项、布尔、日期类型，以及检索、匹配、计算用途；价格明细按字段动态展示和编辑。
- 计算规则的匹配条件改为结构化字段条件，可将价格表字段与工具入参绑定；按量、基础价加增量、阶梯价可选择计费数量字段。
- Excel 导入增加列到价格字段的映射；计价测试展示价格数据、规则和计算过程的实际命中链路。
- 套餐、附加费、折扣、税费、进位及金额上下限等复杂规则暂未进入当前计价 MVP。
- 已使用家政服务报价与超市商品计价两套 mock 数据完成入口、工具编辑、四个页签、导入明细及连续计价结果检查，页面无横向溢出。

## 2026-07-17 语音运行控制配置
- 对话策略新增打断灵敏度、有效短回复词、强制打断词和多条打断后话术，不配置忽略短词。
- 工具新增重复调用、打断后处理、进度反馈、最大执行时间和暂停静默计时。
- 触发器将接口调用与前置数据拉取合并为“调用接口”，支持请求参数及接口返回值到通话变量的映射；旧的 pre_call_fetch 数据编辑时自动兼容为调用接口。
- 大模型配置新增上下文压缩；点击播放录音后进度自动推进并跟随定位对话。
- UI 审查后补充紧凑开关样式与可访问名称，振铃音不参与对话定位，对话气泡支持键盘触发。
- PRD 已按最终讨论同步为打断控制、工具运行控制、调用接口、上下文压缩和播放进度自动跟随五个模块。

## 2026-07-16 知识召回模式与 RAG 工具
- 知识检索新增“全局召回 / 工具调用”模式，旧配置默认全局召回。
- 工具配置新增 RAG 类型，仅配置知识库 ID、Top-K 和相似度阈值。

## 2026-07-16 模型思考开关
- 基础配置的大模型配置区域新增“开启思考”开关，旧配置默认关闭；本期只做 Demo 前端配置，不接模型调用和思考内容过滤。
- Teambition 已创建 RD-3793，备注包含 `<think>`/reasoning 过滤、流式分片、打断、超时和版本兼容要求。

## 2026-07-14 Step / 主题转人工与挂机话术
- Flow Step 和普通主题在原有判断提示词、目标 IVR 基础上，新增“转人工话术”和“挂机话术”多条配置；支持添加、删除，并兼容旧配置未包含新字段的情况。
- 流程主题仍只绑定 Flow，不展示或保存这组配置。

## 2026-07-13 RD-2547 特殊话术配置
- 对话策略新增“特殊话术”卡片，通道确认话术和未听清话术均支持配置多条紧凑单行内容，并兼容旧的单字符串配置；模型、Prompt、阈值及回退逻辑由后端固定。

## 2026-07-10 RD-2442 Step 动作判断配置
- Flow Step 配置新增“转人工与挂机判断”区域，支持填写转人工判断提示词、选择一个目标 IVR、填写挂机判断提示词。
- 三项配置均为可选；普通主题也支持配置同一组转人工与挂机判断字段，流程主题不展示该配置。
- 已补充 Step 配置渲染测试。

## 当前正在做什么
- 已把三个语音 Agent 原型需求落到 React 项目：机器人版本管理、AI 回复日志弹窗、客户记忆后台。

## 上次停在哪里
- 新增 `scripts/check-voice-agent-features.cjs` 需求检查脚本，已通过。
- `npm run build` 已通过，Vite 仅提示现有 bundle 体积较大。

## 近期关键决定和原因
- 版本管理放在机器人列表和机器人配置详情页，不单独开页面，因为它是机器人配置的全局发布治理能力。
- AI 回复日志放在通话详情页的单轮 AI 回复旁，以弹窗查看链路日志。
- 客户记忆作为独立一级菜单和页面，不混入客户画像，避免画像运营和记忆配置职责混杂。

## 2026-06-24 需求补充落地
- 客户记忆已调整为独立一级菜单，二级菜单为「记忆管理」「记忆配置」。
- 「记忆配置」中补充自定义记忆字段管理，字段支持编码、名称、类型、启用、强制抽取、进入 Prompt、脱敏、有效期和抽取说明。
- AI 回复日志补齐输入、理解、流程、知识/工具、Prompt、模型、TTS 等审计字段，并增加工具调用型与知识召回型两类 mock 数据。
- 已验证：`node scripts\check-voice-agent-features.cjs` 全部 PASS；`npm run build` 通过，仅有 Vite chunk 体积提示。

## 2026-06-24 Subagent 审查修复
- 根据 code-reviewer 审查，补齐客户记忆页按钮反馈：重置筛选、开启/关闭记忆、保存草稿、发布配置、停用字段均有本地交互。
- 修复 AI 回复日志复制失败提示，Clipboard 不可用或失败时不再误报已复制。
- 修复 `Pill` 组件类型，相关改动文件在 `npx tsc --noEmit --pretty false` 输出中过滤无新增报错。
- 已验证：`node scripts\check-voice-agent-features.cjs` 全部 PASS；`npm run build` 通过，仅有 Vite chunk 体积提示。

## 2026-06-24 UI 设计规范接入
- 已按 `voice-agent-design-handoff/use.md` 读取设计规范，并将交付包核心文件复制到 `design/`：DESIGN、tokens、组件规范、原型。
- 已在 `src/index.css` 全局接入 `design/tokens/tokens.css`，并补充基础字体、页面背景、focus ring。
- 已在 `tailwind.config.js` 将 primary/sidebar/secondary、字体、圆角、阴影映射到 token；使用 RGB channel 兼容 Tailwind `/opacity` 写法。
- 已按规范升级公共壳层 `components/ui/LayoutComponents.tsx`：侧边栏、顶栏使用 token、保留菜单层级、补充 button 语义与 focus/hover。
- 已按规范升级现有基础表单组件 `components/ui/FormComponents.tsx`：Input、Select、Switch、Slider、TagInput、TextArea 使用 token 并保留原 API。
- 已验证：`node scripts\check-voice-agent-features.cjs` 全部 PASS；`npm run build` 通过，仅有 Vite chunk 体积提示；相关文件在 `tsc` 过滤输出中无新增报错。
- 未提交 git；当前工作区仍含此前未提交的长期流程/文档等改动，需要后续单独处理。

## 2026-06-24 机器人与工具页面 UI 升级
- 已按 `design/` 规范升级「机器人配置列表」：工具栏、搜索、状态筛选、表格密度、状态标签、固定操作列、空态均迁移到 token 体系。
- 已升级「机器人配置详情」外壳：页面容器、版本摘要区、Tabs、更多菜单、版本抽屉和发布弹窗使用 token 化后台组件视觉；未改变保存、发布、版本恢复等业务逻辑。
- 已升级「工具配置」：搜索、分类筛选、工具列表行、状态标签、启停/编辑/删除按钮统一企业后台风格；保留原新增工具、添加 MCP、地理工具编辑逻辑。
- 已修正 `ToolConfigPage` mock 参数缺失 `source` 的类型问题。
- 已验证：`node scripts\check-voice-agent-features.cjs` 全部 PASS；`npm run build` 通过，仅有 Vite chunk 体积提示；相关文件在 `tsc` 过滤输出中无新增报错。
- 未提交 git。
