# 当前进度

## 2026-09-24 计费中心去充值、套餐购买记录分页
- 账户页移除充值和自动充值入口；低余额提醒改为联系客户经理补充额度。历史套餐额度到账仍保留在资金流水与导出里，不删除账务记录。
- 套餐页把重复的套餐卡片和并发批次列表合并为一张购买记录表：每笔独立展示购买时间、路数、实付金额、通话额度、到期日和状态，按购买时间倒序，支持每页 5/10/20 条与翻页。
- 修正续费报价到期日丢失“日”的问题；3 月与 4 月两笔续费各按自己的到期日顺延。计费算术测试通过 85,335 项、静态守卫和生产构建通过。

## 2026-09-24 计费中心统一时间与分批到期
- 演示数据改为 3 月购入 6 路、4 月加购 1 路：当前可用并发 7 路，分别于 2027-03-01 和 2027-04-01 到期。页首将可用并发与余额并列展示，批次到期后自动从可用路数扣除。
- 两笔购买分别生成通话额度 ¥45,000 和 ¥7,500，余额按批次展示：目前分别剩余 ¥42,689.74 和 ¥7,500，各写自己的到期日。合计余额 ¥50,189.74，套餐总额 ¥70,000，已消费 ¥2,310.26。续费按选中的购买批次报价，不混淆到期日。
- 消费页删除月份和最近三个月两套局部时间筛选。页首「消费与流水时间」统一控制消费卡、逐通明细、机器人/月度统计、流水和导出；余额、并发只显示数据截止日的当前值，不随消费时间筛选变化。
- 计费算术测试通过 85,333 项、静态守卫通过、生产构建通过；本地预览服务仍在 8080。

## 2026-09-23 计费中心视觉与页面结构重构
- 去掉计费中心自身的固定高度和内层纵向滚动，页面统一跟随主内容区自然滚动，避免出现类似 iframe 的嵌套页面体验。
- 顶部改为清晰的页面标题、统一时间筛选、四张关键数据卡和一条精简异常提示；六个业务页签保留，但收进同一块紧凑导航中。
- 「余额与额度」拆为账户资产、套餐与并发、提醒与规则三个子页签；余额构成、额度批次、套餐、并发和提醒档位改为响应式卡片，窄屏不再依赖横向滚动条。
- 面板说明改为标题旁问号悬浮查看；规则解释统一默认收起。删掉消费统计的独立“怎么用这张表”区块，资金流水的长说明改为按需展开，提醒正文默认折叠。
- 计费和状态逻辑未改。计费算术测试通过 85,313 项、静态守卫通过、生产构建通过。

## 2026-09-22 通话详情左侧栏改为展示本次通话用到的变量
- 左侧栏重做为两段：最上面是**通话统计**（无折叠态），下面是一组连续排布、折叠逻辑完全相同的板块——客户意向标签、标签、情绪标签、输入变量、对话变量、提取变量、实体。删掉截图里那版原型中的客户画像、对话变量赋值结果和对话路径；这三项在本仓库从未存在过（`git log --all -S"对话路径"` 为空，跨 `agentos`、`voice-agent-new` 等同级目录也没找到），因此是纯新增而非改造。
- 七个板块**共用一个 `renderSection`**，不再各写一套折叠 JSX：标题与右侧箭头一行、点击标题切换、状态存 `expandedSections`、默认展开。抽成函数是为了保证折叠行为不会随修改而分叉，静态测试逐项断言三个标签板块都走了这个函数。
- 变量分类与「变量配置」页的四个页签在 `id`、名称、顺序上**完全一致**（INPUT 输入变量 / CONVERSATION 对话变量 / EXTRACTION 提取变量 / ENTITY 实体）。配置页原先叫「话术输入变量」和「通话变量」，本轮一并改名拉齐——**这推翻了更早那次把「输入变量」明确成「话术输入变量」的决定**（见本文件前面那条「机器人变量页将“输入变量”明确为“话术输入变量”」，保留原文不改写）；配置页的改名为页签、列表标题（「输入变量列表」/「对话变量列表」）和新增按钮文案（「添加输入变量」/「添加对话变量」）三处。静态测试逐项比对两个文件的 id/名称/顺序，任一侧改名或调序都会失败。
- **改名只覆盖「变量配置」页**。`通话变量` 在别处是**另一个功能里的泛指词**，不在本次范围内，仍是旧叫法：触发器「调用接口」的返回值目标下拉（`BotTriggerManager`，且 `voiceRuntimeControls` 测试断言了这个词）、Agent 编排的输出参数映射占位符、漏斗的模拟数据与文案、`docs/语音运行控制配置PRD.md`。要统一需要单独确认，因为那是「变量来源」这个概念的叫法，和「变量分类」不是一回事。
- **只展示本次通话真正用到的变量**：`variableUsages` 记录参与过这通电话的变量及其取值，某一类没有用到的变量时整段不渲染（`if (items.length === 0) return null`），不留空标题。系统变量同此口径——客户选择「只显示真正参与的那几个」，不做全量罗列。
- 每条变量只展示**变量名 + 取值**两项，不再展示英文变量名（`code` 字段已从数据结构和界面一并移除）。两者靠**深浅和字重**分层，让客户第一眼看到的是取值：变量名做成浅灰标签（`bg-slate-100` + `text-[11px]` + `text-slate-500`），取值是深色加重正文（`text-sm font-medium text-slate-800`）；条目间用发丝分隔线（`divide-y divide-slate-100`）而不是每项一个卡片盒子，避免十二条变量堆出十几个方框。刻意不用彩色标签——B 端信息面板不需要靠颜色抢注意力。
- 变量名过长时截断并在 title 里保留全名，取值用 `break-all` 允许任意位置换行。侧边栏宽度固定 `w-64`（内容区 224px），而取值可能又长又没有空格（例如 36 字符的通话 ID），`break-all` 是这里唯一防溢出的手段，已确认 Tailwind 真的输出了该规则而不是被静默裁掉（`first:` / `last:` 这类变体在产物里转义成 `first\:`，用固定字符串核对时要按转义后的形式搜）。
- 变量取值全部是模拟数据：对话变量的名称直接取自配置页 `SYSTEM_VARIABLES` 的中文说明（当前通话 ID、当前对话轮次、当前流程 ID、用户上一轮发言）；输入变量与提取变量在配置页里由客户自定义、仓库中没有对应 fixture，按这通电话的车险场景取名；实体沿用配置页的 `手机号`，另加 `车牌号` 贴合本通话内容（满意度低分反馈正是「没听懂车牌号」），配置页默认实体是「手机号 / 订单号」。
- 未改动右侧对话区、顶部信息栏、满意度调查留痕和「查看日志」入口。

## 2026-09-22 核心模型配置恢复温度采样并扩展快模型与过渡话术
- **恢复温度和核采样的前端入口**，推翻 2026-09-14「核心模型配置精简」中「已移除温度、核采样和上下文压缩的前端入口」的决定；本轮只恢复温度和核采样，上下文压缩与旧版「思考模式」仍在移除之列。这两个字段一直是 `BotConfiguration` 的必填项，且全仓库除 App.tsx 的 fixture 外无人读取，恢复界面不牵动任何数据。
- 入口改为紧凑滑条：`Slider` 新增可选 `compact`，形态是「标签在上 + 40px 输入框内放滑条和取值」，与 `Select`/`Input` 同构（同标签行距、同控件高度），因此并排时底边天然齐平；模型下拉、温度、Top-P 三段并排一行。
- **主模型的名称固定为「主模型」**，不再随双模协同开关在「大模型类型」与「主模型」之间改名——两个名字指同一个模型，客户无法与「快模型」区分；区块标题同理固定为「模型配置」。静态测试锁死「大模型类型」不再出现（ASR 模型名里的「大模型」字样不受影响）。
- 新增加的一路叫「快模型」，复用同一份模型列表并独立配置温度和核采样（`fastModelType` / `fastTemperature` / `fastTopP`，均可选，未配置时按 Gemini Flash / 0.7 / 0.9 兜底）。
- **双模协同开关固定在区块标题行右侧**，改动 2026-09-19「双模协同与思考强度同处一行」的排布：开关切换只增减下方内容，开关自身不再位移。
- 思考强度归属主模型，改名为「主模型思考强度」进一步区分，仍只在双模协同开启时展示，位置从开关行移到主模型下方并与其首列对齐；快模型只做简短快速回应，不配推理深度。
- 新增过渡话术列表（选填，`transitionPhrases`）：主模型给出完整答复前先播报的承接语，预置 3 条（「稍等，我帮您查一下」「那我帮您确认一下」「请稍等」），客户可增删，删空即不播报；话术都很短，两列排布，删除按钮放在输入框内。
- 过渡话术没有复用 `SpeechListEditor`：它在 `value` 为空时强制渲染一行空输入，且删除有「至少留一行」的保护，表达不了「可以完全不填」。改用本文件内的轻量列表，空态只保留「添加话术」入口。
- 读取过渡话术必须用 `??` 而不是 `||`，否则客户删空的 `[]` 会被预置值填回来；静态测试加了一条断言守住这点。
- 与既有等待提示能力的关系：工具级 `playFiller` 仍在工具配置里；`AgentConfig.generalFiller` 是只写不读的死字段。2026-09-19 记录的「首响连接词与双模协同待统一」与本次过渡话术属同类能力，本轮仍未合并。
- 静态守卫测试口径同步更换：温度与核采样从「必须隐藏」改为「必须走紧凑滑条」。

## 2026-09-22 满意度报表与原因追溯收敛
- 原因追溯只保留开关：删掉触发条件、追问话术、原因聚合提示词和原因分类四项逐题配置，触发时机和追问话术由平台按统一口径处理。这是 2026-09-18「每题独立配置固定分类和题目级原因追溯」在原因侧的最后一块——2026-09-21 已删掉开放回答的分类输入，本轮把原因分类一并删掉，客户侧不再有任何分类枚举可填。`types.ts` 里的旧字段保留兼容，不做迁移、不改历史问卷数据。
- 报表去掉满意度趋势：删掉「满意度趋势」区块并移除 `SatisfactionTrendChart` 组件。这是对 2026-09-21 那条新增能力的回退，去掉后报表只剩统计卡、评价项目汇总和调查明细。满意度统计卡保留，核心题口径不变。
- 评价项目汇总删掉「原因追溯」列，评价项目列也不再标注核心满意度题。原因不做聚合，客户原话和归类只在通话记录里逐条查看。
- 评价项目汇总没有可汇总的数据时整块不渲染：删掉「请选择一份调查问卷」占位提示，改成没有回答就这个区块不出现。没选问卷和选了问卷但当前筛选无回答是同一种「没数据」，不再用占位提示区分。
- 调查明细删掉「核心题结果」列：这是对 2026-09-21 那轮的收尾——评价项目列不再标核心题之后，明细里单列一列核心题分数在报表上已经无处呼应，要看就进对应通话记录。列固定为时间、机器人、调查问卷、完成状态、操作。
- 未改动：核心满意度题的选取与满意度计算、问卷完成率、开放回答分类覆盖率、通话记录侧的满意度留痕与原因展示。
- 遗留：通话记录页的原因追溯仍显示归类标签（`reasonCategory`），而配置归类的入口已经删掉。本轮按「只改报表和编辑器」的范围没有动它，是否一并去掉待确认。
- 遗留：追问话术跟着「原因追溯只留开关」一起删了，开关打开后由谁提供追问措辞还没有口径——是平台内置一套统一话术，还是追问话术要作为唯一保留的配置项。原型不接模型调用，界面上看不出差异，但上线前必须定。

## 2026-09-21 满意度报表趋势、题目配置减负与通话留痕打通
- 报表新增满意度趋势图（`SatisfactionTrendChart`）：按天展示调查触发量、问卷完成率和满意度；满意度沿用统计卡口径，只有选中具体问卷且配了核心题时才出这条线。这是对齐 8 家竞品后确认的唯一缺口，逐题流失诊断和开放题原话下钻本轮不做。
- 题目配置减负：评分和枚举题不再展示「回答聚合提示词」；枚举选项的「枚举值 + 展示名称」双输入合并成一个，枚举值退化为内部标识，已有值保持不变，新增项自动补 `option_N`。枚举值必须保持稳定——报表按它对回答做「计为满意」判定，重新生成会让历史满意度归零。
- 开放回答删掉「聚合分类」输入框，只保留聚合提示词，分类改由模型按内容归纳。此处推翻 2026-09-18 的「每题独立配置固定分类」。风险已知：模型自由生成的标签跨期会发散，趋势可比性改由模型侧承担。
- 「总体满意度不好评估」的归因修正：满意度只能由核心封闭题算，开放题不参与，多题也不做加权平均（不同量表不可加）。真正原因是没配核心题，因此新建问卷默认预置一道「总体满意度」评分题并设为核心题。评分量表的三个数字框按产品决定保持现状。
- 打通满意度报表到通话记录的跳转：`CallRecordDetail` 此前完全忽略传入的 `callId`，永远渲染写死的示例通话。现在按 Call ID 解析 `SATISFACTION_RESPONSE_RECORDS`，把该次调查的问答还原成转写，并补上逐题聚合分类、原因追溯和完成进度；演示数据的 Call ID 同步改成 UUID 形状以对得上。
- 报表删掉「查看回答」弹窗，操作列只留「查看通话记录」；客户原话、分类和追溯原因改在通话记录页看，评价项目汇总的聚合分布与原因计数保留。
- 未改动：完成状态筛选与列、评分量表配置、问卷长度提示（按产品决定不加）、原因追溯的提示词与原因分类。

## 2026-09-21 满意度问卷文案与 IVR 题目归属
- 「锁定」口径的文案全部改成客户能看懂的说法，统一为“已发布问卷不能修改，需要调整请复制新建”，涉及列表副标题、编辑页草稿/只读态提示、发布二次确认和开放题分类说明。
- IVR 问卷不再自己保存评价项目：题目和核心满意度题由绑定的 IVR 流程维护，问卷只保存绑定关系；题目定义集中到 `satisfactionData.ts` 的 `SATISFACTION_IVR_FLOWS`，编辑器和报表统一走 `resolveSurveyQuestions` / `resolvePrimaryQuestionId` 解析。
- 新建 IVR 问卷只保留基本信息、关联 IVR 和一行只读提示（显示该流程已配置的评价项目数与核心题），不再支持在问卷里配评价项目。
- 问卷列表删掉「有效回答」列，剩余名称为名称、采集方式、状态、更新时间和操作；此处推翻 2026-08-27 的列表列定义。报表页逐题的有效回答列保留，那是题目级口径，与问卷级静态计数不是一回事。
- 未改动：已发布问卷不可改、复制新建产生新问卷（不体现血缘、名字加「（副本）」）这两条既有行为保持原样。

## 2026-09-19 核心模型配置双模协同
- 原「思考模式」开关升级为「双模协同」：由快、慢两个模型分工应答，快模型先对客户上一句做快速回应，慢模型再输出完整答复，两段拼接后播报，兼顾回应速度与内容完整度。
- 思考强度收敛到双模协同之下，档位为关闭、轻度、深度、最大；「关闭」表示不做深度推理，不代表关闭双模协同，两个状态相互独立。
- 双模协同与思考强度同处一行：开启后在同一输入框内展示四档选项，关闭时收起为「关闭」占位，不再单独占用一行纵向空间。
- 思考强度以 thinkingLevel 为唯一事实来源；旧版 thinkingEnabled 仅在进入编辑页时做一次性迁移，配置界面不再读写，字段按本仓库惯例保留。
- 关闭双模协同时保留原思考强度，重新开启后恢复上次选择，便于反复调试；旧配置按「开关开即双模协同开、强度取原值否则默认轻度」收敛。
- 首响连接词本期不改，其与双模协同在降低等待感上的重叠留待后续统一。
- 静态检查与浏览器复核通过：默认关闭且不展示强度行、开启后默认轻度、两个状态相互独立、保存后往返保持。

## 2026-09-18 满意度问卷与报表口径重构
- 问卷状态收敛为草稿、已发布；已发布问卷只读且不可删除，修改需求通过复制新建处理，避免历史聚合口径漂移。
- 语音智能体问卷支持多评价项目，每题独立配置回答类型、聚合提示词、固定分类和题目级原因追溯。
- 每份问卷选择一个封闭题作为核心满意度题；评分按阈值、枚举按“计为满意”的选项计算，跨问卷不再硬算统一满意度。
- 报表支持 7/15/30 天及自定义时间、机器人、问卷、采集方式和完成状态筛选；逐题展示有效回答、分布、开放题分类覆盖率和原因追溯。
- 调查明细按一次调查展示完成进度，可展开查看全部问题、客户原话、分类及追溯原因，并保留原通话入口。
- 已构造同一机器人 100 次调查数据，覆盖 3 题/5 题、IVR/语音智能体、完整/未完整、评分/枚举/开放回答和原因追溯。

## 2026-09-16 细粒度打断控制原型
- 保留客户熟悉的配置位置：ASR 原“允许被打断”升级为全局打断策略，开场白原开关升级为开场白打断策略，历史布尔字段继续兼容。
- 全局打断与开场白打断为两套独立策略，不存在“跟随”或覆盖关系；前者仅用于普通播报，后者仅用于开场白。
- 每套策略使用“允许打断”开关；开启后，可设置有效表达 1、2、3 次后打断。1 次等同旧版“允许被打断”，2、3 次用于降低误打断。
- “有效表达次数”不属于正常对话轮次：同一段机器人播报期间，每个完成端点识别且通过有效性校验的客户语音片段计 1 次，附和词和背景声不计入。
- 未触发打断的有效表达不丢弃：触发时合并后立即交给模型；机器人先播完时，则在播报结束后合并处理。

## 2026-09-16 RD-6669 打断后未完成输入恢复
- 已在 `docs/语音运行控制配置PRD.md` 补充后端兜底设计，暂不改原型。
- 该问题是打断后最后输入未完成、客户又沉默时造成的等待，不将“打断 + 不完整”定义为不回复。
- 最后一个未完成片段端点记为 M；M+1 秒预生成但不播放，M+2.5 秒静默后播放，任何新有效发言均作废预生成结果。
- 正式上下文只保留客户实际说出和实际听到的内容；未播出的 LLM/TTS 结果及未确认碎片不得写入长期上下文或触发业务操作。

## 2026-09-16 ASR 厂商与模型级联原型
- ASR 识别配置由单一技术型号选择改为「ASR 厂商—识别模型—主语言」三级选择，后台仍保存稳定型号值，兼容原有配置。
- 腾讯模型按提供表格使用客户可理解的名称；模型列表中的问号悬停展示该模型支持的语种，不要求客户理解技术型号。
- 切换厂商会重置为该厂商的默认模型和主语言；切换模型时自动校验主语言，并清理不再适用的候选语言。

## 2026-09-16 Flow 新建交互简化
- Flow 不再维护描述或作为大模型提示词使用，主题匹配是进入 Flow 的依据。
- 点击「新建 Flow」后直接创建默认名称并进入流程编辑；名称在画布顶部直接维护，列表仅保留一个「编辑」入口，并仅按名称检索和展示节点、连线信息。
- 画布顶部的 Flow 名称改为醒目的标题输入区，带编辑图标，避免被误认为只读标题。
- 历史 Flow 的描述数据保留兼容，但不再在流程列表、编辑侧栏和 Flow 引用中展示或传递。

## 2026-09-08 ASR 识别不清主动澄清
- 已整理简洁需求文档 `docs/ASR低置信度主动澄清需求.md`，本轮先讨论方案，暂未改原型。
- 前端只提供“识别不清时主动澄清”开关，客户不配置阈值；平台按 ASR 厂商、模型和语言维护阈值，首轮以 0.80 灰度验证。
- 仅判断最终识别结果；低置信度文本不进入 LLM、工具和信息提取，由系统直接播放固定澄清话术。
- 置信度缺失、无语音和 ASR 失败分别处理；连续澄清最多 2 次，避免无限循环。
- 竞品复审后改为双阈值混合策略：可靠结果正常进入 LLM，中等不确定结果由关闭工具的受限 LLM 结合历史和 ASR 候选生成确认问题，极低分直接请求重述。
- 低置信度文本只作为一次性 `uncertain_input`，不写入长期历史；连续第 2 次改为明确重述提示，第 3 次进入既有全局异常处理。

## 2026-09-08 自研 TTS 编号分段朗读
- TTS 朗读优化新增“号码与编号清晰播报”开关，适用于订单号、手机号、快递单号等连续编号，内部匹配与停顿策略不向客户开放。
- 该能力仅允许自研 TTS 使用；第三方模型下开关不可操作，通过鼠标悬停说明能力与限制。
- 原文本替换规则保持不变，继续兼容历史配置。
- 后端识别、分组和停顿规则记录在 `docs/TTS长编号分段播报说明.md`。
- 为避免反向效果，通用编号增加业务提示词或字段类型约束，并排除 URL、日期、版本号、密钥等内容；任何处理或合成异常均回退原文。
- 前端将旧能力明确命名为“文本替换”和“文本替换规则”，取消容易产生歧义的“词汇列表”和朗读优化总开关；两项能力各自独立开启。
- 后端说明补充候选匹配、结构化停顿片段、字符完整性校验、TTS 安全回退、必测异常和灰度熔断方案；实现原则为任何优化异常均恢复原文，不阻断通话。
- 补充真实通话边界：文本替换后重新计算保护区间；流式文本需缓存未结束的字母数字尾段；音色能力由服务端校验；不同通话不得共享正则和缓冲状态。

## 2026-08-27 实时监控与预警
- 监控报表下新增「实时监控」页面，包含监控和监控预警两个页签。
- 第一阶段指标固定为当前通话、并发使用率、近 5 分钟通话量、通话成功率、异常率、P95 响应耗时和转人工率；依据五家竞品公开资料的行业交集收敛。
- 监控页补充运行趋势、机器人健康状态和可筛选的实时通话列表；耗时异常直接标红并可下钻。
- 预警支持并发、流量、成功率、异常率、P95 耗时、转人工率、工具失败和转人工失败；规则配置范围、阈值、持续时间、等级及邮件/企微/钉钉/Webhook 推送。
- 告警采用同规则同机器人活动事件合并、恢复自动关闭和人工确认机制，避免消息轰炸。
- 需求文档位于 `docs/实时监控与预警需求.md`。

## 2026-08-27 满意度调查与 IVR 融合
- 满意度调查绑定从业务分析页移至机器人触发器，使用“通话结束 + 发送满意度调查 + 已发布调查”完成配置，业务分析页删除原入口。
- 问卷列表收敛为名称、采集方式、有效回答、状态、更新时间和操作；删除指标、问题数和版本。
- 语音智能体调查删除指标类型和语言配置，仅保留基本信息、简短话术、评价项目和可选原因追溯；IVR 调查保留旧版评价项目方式并绑定已发布 IVR。
- 报表收敛为调查触发、完成率、满意度、评价项目汇总和调查明细，不再混合展示 CSAT、NPS、趋势和机器人对比。
- 机器人业务分析中的满意度调查已精简为开关和已发布问卷选择，删除调查比例与评价对象。
- 独立满意度调查继续支持语音智能体和 IVR 两种类型；IVR 类型必须引用现有已发布 IVR，并兼容旧版满意度 IVR。
- IVR 单选题支持逐选项配置按键，不同评价项目可以使用不同按键，同一项目禁止重复按键。
- 报表数据口径改为每个评价项目独立汇总，按键先映射为标准选项，再计算问卷级完成率、CSAT 或 NPS。

## 2026-08-27 满意度调查模块
- 新增独立“满意度调查”模块，包含可检索分页的问卷列表、完整问卷详情和满意度报表。
- 问卷支持 IVR 按键与语音智能体两种采集方式，支持 CSAT 1–5、NPS 0–10、自定义单选/评分/开放回答和低分追问。
- 机器人业务分析新增满意度调查开关，可绑定已发布问卷、配置调查比例和评价对象；滴滴演示机器人已绑定售后 CSAT 问卷。
- AI 通话记录新增满意度调查留痕，展示问卷版本、采集方式、分数、逐题回答和开放反馈主题。
- 报表支持日期、机器人、问卷、指标、结果和关键词筛选，展示完成率、CSAT、NPS、分布、趋势、机器人/问卷对比、大模型主题聚合和明细下钻。
- 已补充 `docs/满意度调查模块设计.md`，记录产品层级、状态、指标口径和竞品取舍。
- 生产构建和满意度调查静态检查均通过；浏览器已走通问卷列表、IVR/NPS 联动、机器人绑定、报表、明细下钻和通话留痕，未发现页面运行错误。
- 本轮检查服务运行在 `http://127.0.0.1:8081/`，原 `8080` 端口已有进程占用。

## 2026-08-26 信息提取方案类型改造
- 机器人业务分析的信息提取页改为接口类型、工具类型二选一；旧配置没有新类型字段时继续按原接口方案读取。
- 接口类型继续选择原信息提取接口方案，删除无实际作用的关联方案详情；工具类型展示当前机器人已加入的工具列表。
- 工具类型明确由大模型根据完整通话和提示词判断是否调用，提示词名称随类型切换为信息提取提示词或工具调用判断提示词。
- 参数统一分为大模型抽取和变量映射：前者可自定义调用参数及提取要求，后者把已有系统/业务变量映射到调用参数。
- 主系统构建通过；本地预览服务已启动在 `http://127.0.0.1:8080/`。
- 工具类型现直接读取左侧「工具配置」的全量共享工具目录，不再限定为当前机器人已加入的工具。
- 已启用工具可直接选择；已停用工具保留展示但不可新增引用，历史已引用项可以解除；被信息提取方案引用的工具禁止直接删除。
- 多选工具时，每条提取参数必须指定目标工具，避免不同工具存在同名参数时发生错误映射；取消选择工具时同步移除该工具的参数映射。
- 方案类型新增“无（仅提取字段）”，只保存大模型抽取结果，不调用接口或工具；切换到该类型时参数统一转为大模型抽取字段。
- 工具选择改为多选下拉，支持同时候选多个工具，由大模型根据通话和判断提示词决定实际调用哪一个或多个。

## 2026-08-26 产品能力与竞品差距盘点
- 已按机器人配置、工具与流程、知识、外呼运营、通话记录、监控、客户运营和通信基础设施逐项检查现有原型。
- 当前判断：页面覆盖已经较广，主要缺口不在继续增加菜单，而在真实执行、上线测试与发布、运行监控、权限审计和数据闭环。
- 下月需求优先围绕统一任务执行底座、机器人测试发布、运行监控、外呼合规、通话后处理、集成可靠性和权限治理，暂不继续扩张低频页面。
- 竞品对照包含 ElevenLabs、Vapi、Retell、Amazon Connect、Genesys、NiCE CXone，以及接口编排场景的 n8n、Postman。
- 根据反馈将差距进一步拆分到具体页面和可独立立项的功能点，覆盖机器人、语音运行、主题与流程、工具、知识、外呼、回呼、通话记录、监控、客户运营、集成、通信资源和系统治理，不再只给平台级摘要。

## 2026-08-26 信息提取与通话后处理方案
- 已将信息提取、通话记录展示、接口传输和可选工具调用整理为独立设计文档，当前只保存方案，暂不改原型。
- 方案将字段提取与通话后处理拆分，后续处理支持仅提取、固定调用工具和模型判断三种方式。
- 文档位置：`docs/信息提取与通话后处理设计.md`；后续从待确认事项继续讨论。

## 2026-08-26 回呼任务执行日志
- 任务执行记录固定展示时间、操作主体、状态和结果，不再把动作名称放在状态列。
- 机器人和系统动作归并为待执行、执行中、已完成；管理员终止时记录操作人、已终止和操作结果。
- 日志使用横向列表，字段较长时可以滚动查看。

## 2026-08-26 回呼计划状态收敛
- 计划状态固定为待执行、执行中、已完成、已终止四种；拨号未接通或系统异常只记录在执行结果中。
- 顶部分类固定为全部、未结束、已结束；未结束包含待执行和执行中，已结束包含已完成和已终止。
- 页面操作统一使用“终止”，终止时不再要求填写原因，系统自动保留操作记录。

## 2026-08-26 回呼计划操作收敛
- 列表操作仅保留查看详情和结束；已结束计划只允许查看详情。
- 批量操作仅保留批量结束，删除编辑、批量调整和再次安排能力。
- 详情页同步删除编辑与再次安排，只在计划仍执行时保留结束操作。

## 2026-08-26 回呼计划列表与详情收敛
- 列表固定为计划编号、计划时间、客户号码、机器人、来源外呼任务、状态和操作，不再展示客户姓名或号码脱敏样式。
- 详情按基本信息、联系单详情、回呼上下文、执行情况和任务执行记录组织，全部使用可扩展的字段列表。
- 联系单详情保留联系单记录编号、客户号码及全部自定义字段；执行后生成的 Call ID 可直接打开对应的 AI 通话记录。

## 2026-08-26 回呼计划详情重构
- 详情页删除“通话链路”和“生命周期记录”等技术化表达，改为来源信息、原联系单内容、本次回呼要求、回呼任务执行情况和任务执行记录。
- 来源信息明确展示来源外呼任务、来源联系单、来源联系人、来源通话记录和计划创建时间。
- 原联系单内容完整保留客户姓名、号码及自定义字段快照；执行区展示机器人、计划时间、实际开始/结束时间、执行通话记录、通话时长、结果和回呼小结。
- 执行记录只描述新回呼任务每一步的时间、执行主体、状态和结果。

## 2026-08-26 回呼取消记录
- 删除模型参数 `cancel_reason`，取消计划只需传递 `action=cancel`。
- 取消来源、操作时间和关联通话由系统自动写入回呼计划的生命周期记录；列表只展示取消状态和最近结果，不增加原因列。

## 2026-08-26 本地预览服务
- 主系统预览服务已重新启动，访问地址为 `http://127.0.0.1:8080/`。

## 2026-08-25 回呼修改取消参数
- 开启修改取消后，模型参数增加 `action`、`execute_time`、`customer_phone` 和 `cancel_reason`，分别支持新建、改期和取消。
- `action` 必填并限定 create、update、cancel；执行时间在新建或改期时必填，取消原因在取消时必填。
- 计划编号不交给模型填写，系统只定位当前通话关联的未执行计划，避免模型操作其他客户计划。
- 关闭修改取消后恢复为执行时间和客户号码两个必填参数；外呼任务与联系单方式继续不展示任何参数和修改取消开关。

## 2026-08-25 回呼参数与计划操作收敛
- 加入指定外呼任务并选择联系单后，不再展示或配置模型参数，号码和客户信息全部由联系单提供。
- 当前机器人和指定机器人方式的参数改为紧凑四列定义，仅展示模型参数、参数名、类型和必填说明。
- 不开放计划查询；增加“允许修改或取消计划”开关，默认开启，仅允许模型操作本次通话关联且尚未执行的计划，避免重复新建和越权修改。

## 2026-08-25 回呼执行方式扩展
- 回呼方式支持当前机器人、选择其他机器人、加入指定外呼任务三种方式，默认使用当前机器人。
- 选择其他机器人时由客户预先指定已发布机器人，不允许模型临时选择执行资源。
- 加入外呼任务时级联选择外呼任务和对应联系单；客户号码由联系单带入，执行时间仍由模型识别或使用配置默认值。
- 当前机器人和指定机器人方式均保留必填执行时间与客户号码，兼顾默认开放能力和固定业务落点。

## 2026-08-25 本地预览服务
- 主系统预览服务已重新启动，访问地址为 `http://127.0.0.1:8080/`。

## 2026-08-25 回呼工具配置精简
- 工具配置仅保留执行机器人和两个必要参数，不再展示内部实现说明。
- 执行机器人默认使用当前机器人，也可选择其他已发布机器人；删除话术、模型白名单和开放能力配置。
- 参数仅保留必填执行时间和客户号码，支持配置默认值；删除自定义参数、系统快照、来源链路和标准返回等技术信息展示。
- 回呼计划后台仍保留完整生命周期和审计能力，但不把这些内部细节暴露给工具配置客户。

## 2026-08-25 回呼全生命周期与工具开放性复审
- 回呼能力从单一“创建计划”扩展为创建、查询、改期和取消四个独立模型函数，避免在一个函数内混用不同参数。
- 执行机器人和话术均支持当前配置、固定指定、模型从管理员白名单选择三种方式；模型不能填写任意资源 ID。
- 调用参数增加模型提取、系统带入、引用变量和固定值四种来源，并保留默认值和校验规则；联系单快照、回呼上下文和来源链路继续由系统锁定。
- 工具返回计划编号、实际时间、客户号码、机器人和状态，模型可据此继续调用短信、工单等其他工具，回呼工具本身保持单一职责。
- 回呼计划增加待执行、排队中、呼叫中、通话中、完成、失败、取消和过期状态，并按待执行、执行中、已结束分组管理。
- 详情将来源任务、来源通话、回呼计划、执行通话和后续计划串成完整链路；失败或完成后重新安排会生成新计划，旧记录不覆盖、不自动无限重呼。
- 批量调整仅允许待执行计划，人工可结束待执行或执行中的计划；所有动作保留原因和生命周期记录，业务与通话记录不允许删除。
- 参考 ElevenLabs 的机器人、动态变量、计划时间和逐通话状态管理，以及 Amazon Connect 的独立回呼联系、排队、执行前校验和终止机制完成本轮复审。
- 主系统构建通过；浏览器检查回呼列表、状态分组、详情链路和工具配置通过。

## 2026-08-25 回呼工具与计划商用化
- 预约回呼工具只创建 AI 回呼计划，默认使用当前机器人；删除人工回呼、时区、预约范围和重试等非核心配置。
- 模型参数固定包含必填 `execute_time` 和 `customer_phone`，均支持默认值；客户号码默认 `{{system.customer_phone}}`，并允许增加带类型、必填、默认值和说明的自定义参数。
- `contact_info` 由系统保存联系单快照，`callback_context` 由系统总结预约缘由和待办并在回呼时拼接到 LLM 提示词，两项均不可配置。
- 回呼计划列表补齐日期范围、状态、机器人、来源任务、结果关键词筛选、时间排序、勾选和分页；核心列展示时间、手机号、机器人版本、话术、来源任务、状态和最近结果。
- 待执行计划支持单条编辑；批量调整仅作用于待执行计划，可整体偏移时间并切换机器人或话术；待执行和执行中计划支持填写原因后人工终止。
- 支持导出已选记录或当前筛选结果；不提供删除，所有编辑、终止和执行结果保留操作人、时间、动作和说明。
- 详情页展示联系单基本信息快照、回呼上下文、回呼通话小结及完整状态操作记录。
- 构建及工具参数、筛选、单条编辑、批量调整、批量终止和详情联动检查通过。

## 2026-08-25 ASR 多语言配置
- ASR 配置新增必填主语言，所有模型均可选择语言。
- 新增 AWS Transcribe 模型；AWS 与微软 Azure STT 支持在主语言后添加最多 3 个候选语言，总计最多 4 种。
- 候选语言按优先级排列，禁止与主语言或其他候选语言重复；切换到不支持候选语言的模型时自动清空候选项。
- 已完成构建和页面交互检查，三条候选语言上限及按钮禁用正常。
- 根据页面反馈压缩为两行布局，删除候选语言说明卡、重复提示和空状态，只保留必要字段与添加操作。

## 2026-08-25 预约回呼原型落地
- 工具配置新增平台内置“预约回呼”类型，统一支持创建、查询、改期和取消；后续已收敛为仅 AI 回呼和模型参数配置。
- 机器人业务分析保留旧信息提取方案兼容，同时将通话后执行范围扩展为当前机器人全部工具；每个工具可单独启用并配置执行条件。
- 通话小结补齐自动生成、作为回呼上下文及回呼后生成新小结三项配置；来源小结与变量快照不会被后续机器人修改覆盖。
- 外呼任务下新增“回呼计划”，支持关键词、计划日期、状态、机器人和来源任务筛选、分页示意与详情查看；详情展示来源通话、执行结果和回呼小结。
- 关键决定：预约回呼与普通未接通重试分开管理；同一客户未执行计划默认更新，超过最晚执行时间后停止，避免重复计划和无限循环。
- 主系统构建通过；页面联动检查通过。

## 2026-08-25 预约回呼产品设计
- 明确“预约回呼”和“自动重呼”是两类对象：预约回呼来自客户或机器人对未来通话时间的明确承诺；自动重呼只处理忙线、无人接听等失败结果。
- 预约回呼不创建新的批量外呼任务；在外呼模块增加“回呼计划”列表，每条记录对应一个客户、一个计划时间和一种执行方式，底层复用现有号码、并发、黑名单、工作时间和拨号能力。
- 机器人侧提供平台级“回呼计划”工具能力，统一支持创建、查询、改期和取消；工具配置执行模板、AI/人工执行方式、时间限制、确认规则和失败策略。
- 通话中由模型提取并归一化时间，系统校验后返回实际生效时间供机器人复述；通话后只对存在明确双方承诺且时间确定的记录自动创建，其余进入待确认状态。
- 回呼计划固定已发布机器人版本和本次通话变量快照；活动回呼期间暂停同一联系人的普通重呼，回呼失败次数和最晚执行时间必须有限，避免循环。
- 产品方案已在后续“预约回呼原型落地”阶段实现。

## 2026-08-25 外呼联系单自定义字段与机器人输入变量
- 联系单列表增加字段数量和联系人明细入口；明细按联系单字段动态生成客户昵称、性别、年龄、城市等列，并支持全文检索、列设置和分页。
- 机器人变量页将“输入变量”明确为“话术输入变量”，用于声明稳定变量名、类型、默认值和是否必填；话术通过 `{{variable_name}}` 引用。
- 外呼任务继承模板绑定的机器人及版本，并按每个联系单将原始字段映射到机器人输入变量；必填映射缺失时禁止保存任务。
- 任务详情增加“输入变量”页签，可回看机器人版本、联系单来源字段和话术引用；联系单列表可继续下钻查看联系人动态列。
- 关键决定：联系单保存原始客户数据，任务承担字段适配，机器人只依赖输入变量契约；变量值只对当前联系人通话生效，不跨联系人共享。
- 已完成主系统构建和页面联动检查，动态列、任务映射与任务详情回看正常。

## 2026-08-25 信息提取鉴权与接口工作流
- 鉴权管理补齐名称、描述、提供方和鉴权类型检索，以及总数、空结果和每页 10 条分页，使接口、鉴权、工作流三类资源保持一致的列表管理体验。
- 明确接口与工作流的两层组织：接口负责单次上游调用，工作流通过服务输入、前序步骤输出和固定值串联接口，发布后形成统一调用服务。
- 按产品反馈删除“对外调用”整块配置及“已开放 API”状态；已发布工作流仅作为系统内完整“调用服务”供机器人或其他功能引用。
- 删除工作流步骤对机器人变量的依赖，确保不同系统内调用方使用同一输入契约；新增发布校验，检查字段重复、映射缺失、逆序引用和跳过依赖步骤。
- 接口列表和工作流列表均补充名称/描述查询、总数、空结果和每页 10 条分页；工作流列表展示草稿与可引用状态。
- 参考 n8n 和 Postman 官方文档完成本轮设计；主系统构建通过，浏览器检查接口列表、工作流列表、编辑和校验均正常。
- 信息提取页只保留「接口管理、鉴权管理、接口工作流」三个页签，删除触发器设置入口。
- 接口页统一使用“接口”命名，列表提供编辑、复制、删除；详情保留接口测试、预置模板和旧鉴权兼容，并可引用独立鉴权。
- 独立鉴权严格支持 Basic、Bearer、OAuth2 Client Credentials、OAuth2 JWT 四种类型；新建和编辑使用独立子页面，不使用侧滑栏。
- 旧接口没有新字段时自动进入兼容模式，继续使用原 `authType` 和原配置，不做强制迁移。
- 工作流改为列表管理；工作流先定义整体服务标识、输入和输出，再配置顺序步骤、接口入参来源、步骤输出、超时和失败策略。
- 已发布工作流与单个接口统一作为“调用服务”供其他功能引用；步骤可引用任意前序步骤输出，最多重试 3 次且不允许回环。
- 已完成主系统构建和浏览器检查，三页结构、四类鉴权、旧接口兼容、工作流服务契约和步骤映射均正常；本地预览运行在 8080 端口。
- 按线上旧页面截图恢复接口详情的原有鉴权布局：直接保留“基本身份验证 / URL 鉴权”，仅追加“引用已有鉴权”，不再用新的兼容模式下拉替换旧交互。
- URL 鉴权补齐签名参数表，参数类型严格包含常量、变量、时间戳、随机字符串，并可配置 URL 编码、sha256 和签名参数名。
- OAuth2 JWT 补齐访问令牌/ID 令牌，以及 HS256、HS384、HS512、RS256、RS384、RS512 六种算法；同时保留令牌 URL、范围、密钥 ID、有效期、iss、aud、sub 和额外 JSON 参数。

## 2026-08-24 Gemini TTS 接入需求评审
- 新增 `docs/Gemini_TTS_接入需求.md`，将调研记录整理为可开发、可测试、可验收的正式需求。
- 语音机器人选择 Cloud Text-to-Speech API，唯一模型确定为 Gemini 2.5 Flash，不开放 Pro、Flash Lite 或 3.1 Preview。
- 纠正“音色自动检测语言”的表述：每次请求显式传语言代码；混合语言使用句子主语言，并由菲律宾真人实测验收。
- 一个请求只能传一个 `languageCode`；混合语言优先整句提交并指定主语言，提示模型保留其他语言原读法，不达标时再按完整语言句拆分并保持同一音色。
- 纠正 Charon、Enceladus、Iapetus 被中文网页翻译的问题，接口一律使用英文音色 ID；补齐 24 个 GA 语言和 63 个 Preview 语言。
- 宿务语和菲律宾语均为 Preview，上线必须通过海外菲律宾真人盲听、8 kHz 电话链路、延迟、QPM 容量、成本和降级验证。

## 2026-08-24 智能接听识别与处理
- 配置放在单个机器人「对话策略」页签最底部，不新增独立页签。
- 仅保留 IVR 智能导航、语音留言和 AI 助手接听，删除占线、暂时无法接通和空号等未接通结果处理。
- 语音留言支持留言后挂机或直接挂机，后续操作可选择无或进入当前任务下一轮重呼。
- AI 助手采用两阶段处理：当前通话可进入指定 IVR 流程或直接挂机；后续操作可选择无、重呼或加入指定外呼任务，并级联选择联系单。
- 页面语言统一为“当前通话处理 / 后续操作”；删除“仅记录识别结果”，并将“转入外呼任务”改为更准确的“加入外呼任务”。
- 语音留言与 AI 助手的后续操作保持一致，均支持无、延时重呼、加入指定外呼任务；选择重呼后必须填写多少分钟后执行，默认 30 分钟。
- 需求复审后补充防循环：延时重呼必须配置次数上限，默认 2 次、最大 10 次；同一联系人最多跨任务流转一次，已处理任务不允许再次加入。
- 删除 AI 助手“进入指定 IVR 流程”：该动作与对端 AI 接听没有直接关系，改为播报说明后挂机或直接挂机；只有高置信度识别才执行，无法确认时按真人接听继续。
- 重呼和任务流转继续受外呼任务全局触达上限、可呼叫时段及黑名单约束，局部策略不能绕过任务级安全限制。
- 智能接听区域改为紧凑三行列表，默认只显示策略摘要和开关；语音留言与 AI 助手点击配置后展开，同一时间仅展开一项，减少对话策略页面占用空间。
- 关键决定：目标 IVR 表示切换到本平台专用通话流程，不表示转人工队列；实时通话处理与通话后的任务动作分开配置。
- 已验证：主系统构建通过；浏览器确认配置位于对话策略底部，IVR 显隐、挂机切换及外呼任务/联系单级联正常。

## 2026-08-23 音色市场重设计
- 音色市场收敛为简单列表，支持自研音色、MiniMax、豆包、Google、ElevenLabs 五个厂商。
- 支持关键词、厂商、中文/英文/阿拉伯语/印尼语/泰语/马来西亚语及性别筛选。
- 每行只展示音色名称与 ID、厂商、语种、性别、基础标签，最右侧提供播放/停止按钮；不再提供详情面板、自定义文本和音色选择。

## 2026-08-23 本地服务
- 主系统与操作手册实时预览服务已启动，可继续修改原型和 Markdown 文档。

## 2026-08-20 本地服务
- 主系统与操作手册实时预览服务已重新启动，主站使用 5173 端口，文档开发服务使用 5174 端口。

## 2026-08-18 操作手册实时预览
- `npm run dev` 现在同时启动主系统与文档开发服务，访问地址仍统一为主站下的 `/docs`。
- 修改 `manual/docs/` 中的 Markdown 并保存后，操作手册会自动编译并刷新；正式构建方式保持不变。

## 2026-08-18 操作手册返回路径优化
- 检查发现窄屏下顶部“返回主系统”会隐藏，移动目录中也没有返回入口。
- 在文档左侧目录首位增加“返回主系统”，窄屏展开目录后同样可见；桌面端顶部保留按钮式返回入口。
- 精简文档顶部导航，移除重复的“文档/博客”入口，并统一页脚品牌文案，降低主系统与操作手册之间的割裂感。

## 2026-08-18 操作手册融合
- 将 `Hill-Z/voice_agent_docs` 的 Docusaurus 源码纳入主仓库 `manual/`，保留配置指南、外呼任务、API、常见问题、博客等内容。
- 文档以 `/docs` 子路径构建并随主项目统一输出；根项目新增工作区和 `build:docs`、`build:app` 命令，`npm run build` 会完成整站构建。
- 主系统侧边栏新增「帮助与支持 / 操作手册」，入口和 `/docs` 均直接进入「快速开始」正文；文档顶部保留「返回主系统」。
- 文档顶部新增中文全文搜索框，本地索引覆盖标题与正文，不依赖外部搜索服务。
- 已验证：文档构建通过；浏览器确认快速开始、左侧目录、中文搜索和返回主系统正常，搜索“变量配置”返回 8 条结果。

## 2026-08-18 主题匹配上下文配置
- 单个机器人的「主题管理」页拆分为上方默认收起的「主题匹配设置」和下方原主题列表，原有增删改查、筛选、排序与分页保持不变。
- 新增「历史对话辅助匹配」开关和历史消息条数，机器人与用户消息统一按条计数；默认开启并读取最近 3 条，可配置 1–50 条。
- 新增「上一轮主题优先匹配」开关；开启后优先判断当前输入是否延续上一轮主题，不匹配时再识别其他主题，默认关闭。
- 按页面反馈将展开内容压缩为两行配置，条数输入与开关同行，说明文案各保留一句。
- 已验证：构建和 2 项需求检查通过；本地页面完成默认收起、展开、开关联动、默认值和原主题列表检查。

## 2026-08-18 本地服务启动
- 已按 `package-lock.json` 安装前端依赖，并统一使用 npm 官方源完成下载。
- `npm run build` 已通过；本地 Vite 服务已启动在 `http://127.0.0.1:5173/`。
- 当前 Node.js 为 20.11.1，依赖提示建议升级到 20.19.0 或更高版本，但不影响本次页面启动。
- 项目开始使用 Git 管理，首次版本关联仓库 `Hill-Z/voice_agent_prototype`。

## 2026-08-04 机器人级多 Agent 编排
- 机器人配置新增「Agent 编排」页签，简单场景保留单 Agent，复杂场景支持 Supervisor 统一调度与 Handoff 直接接力。
- 多 Agent 使用现有 Flow、旧版意图流程和普通主题作为对话 Agent 来源；任务 Agent 通过任务提示词、多工具、结构化输出和超时配置静默执行，不接管 TTS。
- 编排画布支持节点拖动、缩放、平移、连线、节点/边删除，以及入口 Agent、兜底 Agent 和最大转交次数配置。
- Flow/主题 Agent 默认继承关联资源，也可覆盖工具和知识库；人工目标从机器人已有 IVR 配置汇总；挂机节点配置多条结束话术。
- Supervisor 连线表达能力分派，仅配置可选准入条件；Handoff 连线配置触发条件和优先级；委派、并行、转人工和兜底分别使用独立配置字段及校验规则。
- Supervisor 与 Handoff 各自保存独立画布草稿，切换模式不会删除或改写另一模式的节点和连线。
- 编排保存纳入机器人统一草稿与版本发布，不单独建立应用或独立发布体系；旧机器人默认保持单 Agent。
- 已验证：`npm run build` 通过；浏览器完成 Supervisor 准入条件、旧版流程资源、任务 Agent 配置和 Supervisor/Handoff 草稿隔离检查。

## 2026-08-03 自动跟进菜单与 Flow 管理
- 自动跟进调整为一级菜单，下设「跟进任务」「规则编排」「跟进记录」三个二级菜单，移除页面内页签切换。
- 跟进任务支持新建和编辑，并关联已发布的跟进 Flow；任务保存时记录 Flow 名称和版本。
- 规则编排先进入 Flow 列表，支持新建、编辑、复制、发布、停用和删除，再进入独立无限画布编辑；已关联任务的 Flow 不允许删除。
- 跟进记录按单次执行明细展示任务、客户、Flow 版本、执行次数、结果和结果说明。
- 手动完成和转人工会同步生成执行记录；历史任务始终展示创建时的 Flow 名称和版本。

## 2026-07-22 业务事件与业务漏斗
- 监控报表保留原有 Topic / Flow 统计，新增独立“业务漏斗”工作区，避免重复实现 Flow 路径分析。
- 业务事件支持平台事件、Tool 返回、接口回调、通话变量四类来源，Tool / 回调 / 变量使用结构化字段、条件和值定义；事件可新增、编辑、启停，已被漏斗使用的事件不可删除。
- 漏斗支持选择机器人和版本、添加/删除/上下移动阶段，每个阶段必须绑定已启用业务事件；统计使用通话事件严格按顺序计算，同一通话和阶段只计一次。
- 漏斗以宽度递减的真实漏斗展示到达量、上一步转化率、累计转化率和流失量；点击阶段或流失量可查看对应通话记录与流失原因。
- 当前为前端原型和 mock 数据，业务成功只通过 Tool 返回、接口回调或变量条件事件记录，不由模型推断。
- 已验证：`npm run build` 通过；本地页面已检查事件列表、配置弹窗、漏斗阶段和通话下钻。

## 2026-07-20 安全护栏、工具分级与异常策略
- 对话策略新增固定开启的协议护栏，配置敏感信息处理、业务禁答规则和多条安全兜底话术；流式解析器过滤隐藏、未知和未闭合标签，并将转人工/挂机标签拆为动作。
- 工具编辑新增默认执行方式和按参数判断的执行规则，支持自动执行、用户确认、人工审批、仅人工处理，并配置确认话术、人工队列、IVR和审批超时。
- 异常处理支持机器人全局兜底动作、IVR、连续异常上限和多条话术；Flow Step 可继承全局或覆盖为继续、跳转、转人工、挂机。
- 兼容 `#Transfer[ivr:<id>]` 与旧客户常用的 `#Transfer[<id>]`；工具规则只对声明为数字的参数做数值比较，字符串订单号保持精确匹配；短回复词和强制打断词支持保存为空；工具变量引用增加可选变量池和空引用校验。
- 新增静态检查与可执行行为测试，完成真实页面验证；原型截图保存在系统临时目录 `voice-agent-tb-new`。
- 已在 TB 创建并附截图：RD-3897（安全护栏）、RD-3898（工具调用分级与人工处理）、RD-3899（全局与 Step 异常处理策略）。

## 2026-07-20 流程与主题列表筛选
- 流程配置列表新增按名称或描述搜索，并支持清空及无结果状态。
- 主题管理新增流程/普通分类筛选和分类列，保留原有搜索、状态、排序和分页。

## 2026-07-22 监控报表漏斗与业务事件配置入口
- 监控报表拆分为「综合报表」「漏斗配置」「事件配置」三个二级菜单，原监控报表页面保留为综合报表。
- 新增漏斗配置页：展示漏斗名称、绑定机器人、版本、阶段、启用状态，并支持进入漏斗编辑面板调整阶段顺序、事件和启用状态。
- 新增业务事件配置页：展示事件名称、事件编码、来源、来源对象、条件、启用状态，并支持编辑事件定义。
- 综合报表中的业务漏斗保留真实漏斗图、阶段人数、转化率、流失人数和通话明细下钻，配置页与分析页职责分离。
- 已在 `artifacts/business-funnel-rd/` 保存综合报表、漏斗配置、漏斗编辑、事件配置、事件编辑 5 张原型截图。
- `npm run build` 已通过，Vite 仅提示现有 bundle 体积较大。

## 2026-07-22 报价与商品计价 MVP
- 报价与商品计价统一为「智能计价」工具类型，通过单次报价、连续计价、同时支持三种使用方式区分运行行为；保留旧 QUOTE/CART_PRICING 配置读取兼容。
- 智能计价工具保留原有「编辑」入口，用于维护工具名称、函数名称、描述、模型使用指南、参数、调用控制和运行控制；列表额外提供「配置计价」入口。
- 计价配置支持平台计价与客户接口两种来源。平台计价内部包含价格数据、计算规则、导入记录、计价测试四个页签；客户接口仅保留接口配置和计价测试。
- 价格数据支持价格表版本下拉、编码/名称/规格搜索、分类/区域或门店/状态筛选、20/50/100 条分页、Excel 导入、新增和编辑价格。
- 计算规则支持搜索、计价方式/状态筛选、分页及固定价、按量计价、基础价加增量、阶梯价结构化编辑。
- 导入记录支持文件名/操作人搜索、状态筛选、分页；导入结果支持逐行搜索、结果筛选、分页和失败数据下载。
- 计价测试分别提供单次报价试算与连续购物车计价，展示命中规则、计算明细和合计金额。
- 价格表支持维护自定义字段，字段可配置文本、数值、选项、布尔、日期类型，以及检索、匹配、计算用途；价格明细按字段动态展示和编辑。
- 计算规则的匹配条件改为结构化字段条件，可将价格表字段与工具入参绑定；按量、基础价加增量、阶梯价可选择计费数量字段。
- Excel 导入增加列到价格字段的映射；计价测试展示价格数据、规则和计算过程的实际命中链路。
- 套餐、附加费、折扣、税费、进位及金额上下限等复杂规则暂未进入当前计价 MVP。
- 已使用家政服务报价与超市商品计价两套 mock 数据完成入口、工具编辑、四个页签、导入明细及连续计价结果检查，页面无横向溢出。

## 2026-07-17 语音运行控制配置
- 对话策略新增打断灵敏度、有效短回复词、强制打断词和多条打断后话术，不配置忽略短词。
- 工具新增重复调用、打断后处理、进度反馈、最大执行时间和暂停静默计时。
- 触发器将接口调用与前置数据拉取合并为“调用接口”，支持请求参数及接口返回值到通话变量的映射；旧的 pre_call_fetch 数据编辑时自动兼容为调用接口。
- 大模型配置新增上下文压缩；点击播放录音后进度自动推进并跟随定位对话。
- UI 审查后补充紧凑开关样式与可访问名称，振铃音不参与对话定位，对话气泡支持键盘触发。
- PRD 已按最终讨论同步为打断控制、工具运行控制、调用接口、上下文压缩和播放进度自动跟随五个模块。

## 2026-07-16 知识召回模式与 RAG 工具
- 知识检索新增“全局召回 / 工具调用”模式，旧配置默认全局召回。
- 工具配置新增 RAG 类型，仅配置知识库 ID、Top-K 和相似度阈值。

## 2026-07-16 模型思考开关
- 基础配置的大模型配置区域新增“开启思考”开关，旧配置默认关闭；本期只做 Demo 前端配置，不接模型调用和思考内容过滤。
- Teambition 已创建 RD-3793，备注包含 `<think>`/reasoning 过滤、流式分片、打断、超时和版本兼容要求。

## 2026-07-14 Step / 主题转人工与挂机话术
- Flow Step 和普通主题在原有判断提示词、目标 IVR 基础上，新增“转人工话术”和“挂机话术”多条配置；支持添加、删除，并兼容旧配置未包含新字段的情况。
- 流程主题仍只绑定 Flow，不展示或保存这组配置。

## 2026-07-13 RD-2547 特殊话术配置
- 对话策略新增“特殊话术”卡片，通道确认话术和未听清话术均支持配置多条紧凑单行内容，并兼容旧的单字符串配置；模型、Prompt、阈值及回退逻辑由后端固定。

## 2026-07-10 RD-2442 Step 动作判断配置
- Flow Step 配置新增“转人工与挂机判断”区域，支持填写转人工判断提示词、选择一个目标 IVR、填写挂机判断提示词。
- 三项配置均为可选；普通主题也支持配置同一组转人工与挂机判断字段，流程主题不展示该配置。
- 已补充 Step 配置渲染测试。

## 当前正在做什么
- 分支 `feat/billing-center`：独立的「计费中心」一级页签（余额与额度 / 资金流水 / 通话消费明细 / 消费统计 / 通知记录 / 报表导出），并在机器人发布页加入实时价格预览。

## 上次停在哪里
- 计费中心阶段一至阶段三（余额改批次制 / 资金流水与智呼口径 / 信用额度·欠费提醒·报表导出）实现完成，PRD 与《计费引擎规格》在第七轮统一改写收口，README 补记两条重要设计决定。
- `node tests/billingCenter.money.mjs` 通过 85313 项、0 失败（用 esbuild 把 TS 打进内存再导入，真算不查字符串）；`node tests/billingCenter.static.mjs` 通过；21 个 `.static.mjs` 全过；全部 `.mjs` 测试 1 失败（`safetyHitlException.behavior.mjs` 读的是别的机器上的绝对路径 `C:/Users/13609/.trae-cn/AI-voice-bot`，本轮之前就失败，与计费无关）；`node scripts/check-voice-agent-features.cjs` 27 项通过；`npx tsc --noEmit` 88 行与基线一致；`npm run build:app` 通过。
- 补充：`tests/` 下的 `.ts` / `.tsx` 用例（8 个 render / behavior 文件）不能用裸 `node` 直接跑（`ERR_UNKNOWN_FILE_EXTENSION`），需要 loader；本轮没有跑它们，改动也不涉及它们覆盖的模块。
- 未提交 git。

## 近期关键决定和原因
- 版本管理放在机器人列表和机器人配置详情页，不单独开页面，因为它是机器人配置的全局发布治理能力。
- AI 回复日志放在通话详情页的单轮 AI 回复旁，以弹窗查看链路日志。
- 客户记忆作为独立一级菜单和页面，不混入客户画像，避免画像运营和记忆配置职责混杂。
- 计费中心独立成一级页签，不并入监控报表：报表回答「打得好不好」，计费回答「钱花在哪」，两者的权限和读者都不同。

## 2026-06-24 需求补充落地
- 客户记忆已调整为独立一级菜单，二级菜单为「记忆管理」「记忆配置」。
- 「记忆配置」中补充自定义记忆字段管理，字段支持编码、名称、类型、启用、强制抽取、进入 Prompt、脱敏、有效期和抽取说明。
- AI 回复日志补齐输入、理解、流程、知识/工具、Prompt、模型、TTS 等审计字段，并增加工具调用型与知识召回型两类 mock 数据。
- 已验证：`node scripts\check-voice-agent-features.cjs` 全部 PASS；`npm run build` 通过，仅有 Vite chunk 体积提示。

## 2026-06-24 Subagent 审查修复
- 根据 code-reviewer 审查，补齐客户记忆页按钮反馈：重置筛选、开启/关闭记忆、保存草稿、发布配置、停用字段均有本地交互。
- 修复 AI 回复日志复制失败提示，Clipboard 不可用或失败时不再误报已复制。
- 修复 `Pill` 组件类型，相关改动文件在 `npx tsc --noEmit --pretty false` 输出中过滤无新增报错。
- 已验证：`node scripts\check-voice-agent-features.cjs` 全部 PASS；`npm run build` 通过，仅有 Vite chunk 体积提示。

## 2026-06-24 UI 设计规范接入
- 已按 `voice-agent-design-handoff/use.md` 读取设计规范，并将交付包核心文件复制到 `design/`：DESIGN、tokens、组件规范、原型。
- 已在 `src/index.css` 全局接入 `design/tokens/tokens.css`，并补充基础字体、页面背景、focus ring。
- 已在 `tailwind.config.js` 将 primary/sidebar/secondary、字体、圆角、阴影映射到 token；使用 RGB channel 兼容 Tailwind `/opacity` 写法。
- 已按规范升级公共壳层 `components/ui/LayoutComponents.tsx`：侧边栏、顶栏使用 token、保留菜单层级、补充 button 语义与 focus/hover。
- 已按规范升级现有基础表单组件 `components/ui/FormComponents.tsx`：Input、Select、Switch、Slider、TagInput、TextArea 使用 token 并保留原 API。
- 已验证：`node scripts\check-voice-agent-features.cjs` 全部 PASS；`npm run build` 通过，仅有 Vite chunk 体积提示；相关文件在 `tsc` 过滤输出中无新增报错。
- 未提交 git；当前工作区仍含此前未提交的长期流程/文档等改动，需要后续单独处理。

## 2026-06-24 机器人与工具页面 UI 升级
- 已按 `design/` 规范升级「机器人配置列表」：工具栏、搜索、状态筛选、表格密度、状态标签、固定操作列、空态均迁移到 token 体系。
- 已升级「机器人配置详情」外壳：页面容器、版本摘要区、Tabs、更多菜单、版本抽屉和发布弹窗使用 token 化后台组件视觉；未改变保存、发布、版本恢复等业务逻辑。
- 已升级「工具配置」：搜索、分类筛选、工具列表行、状态标签、启停/编辑/删除按钮统一企业后台风格；保留原新增工具、添加 MCP、地理工具编辑逻辑。
- 已修正 `ToolConfigPage` mock 参数缺失 `source` 的类型问题。
- 已验证：`node scripts\check-voice-agent-features.cjs` 全部 PASS；`npm run build` 通过，仅有 Vite chunk 体积提示；相关文件在 `tsc` 过滤输出中无新增报错。
- 未提交 git。

## 2026-08-27 实时监控与预警
- 在「监控报表」下新增「实时监控」，包含运行看板、机器人健康状态、实时通话、告警规则和告警记录。
- 监控指标分为实时状态和今日概览；今日补齐呼入/呼出、平均时长、转人工量与比率、工具调用量与成功率、工具 P95 耗时。
- 告警支持机器人范围、阈值、持续时间、生效时间、等级及邮件/企业微信/钉钉/Webhook；比例指标固定使用最小样本，告警自动恢复并去重。
- 已逐项核对 PolyAI、Retell AI、Cognigy、Kore.ai、Sierra 详细看板，并以腾讯云补充电话话务口径，结论写入 `docs/实时监控与预警需求.md`。

## 2026-09-08 ASR 置信度厂商调查
- 调查结果已补充到 `docs/ASR低置信度主动澄清需求.md`：微软、AWS 可直接接入；腾讯、ElevenLabs、Fano 需按现网接口联调确认；Paraformer、Qwen3-ASR 暂未公开文字置信度；弧光 ASR 缺少可核验资料。
- 关键决定：语言概率、情绪置信度不能当作文字识别置信度；阈值按厂商、模型和语言分别管理。

## 2026-09-11 开场白打断策略讨论
- 建议把二元开关升级为三种策略：允许直接打断、连续表达后打断、始终不允许打断；连续表达默认 2 次。
- “连续次数”按同一段开场白中的有效抢话计算，不占用正常对话轮次；明确的停止、挂断、拒接等指令可立即生效。
- 未达到提前打断条件的有效语音也不能丢弃：先缓存，开场白结束后再处理；多段内容合并时保留有效信息、以客户最新意图为准。
- 已补充调研 ElevenLabs、PolyAI、Retell AI、Vapi、LiveKit、Hume、Cognigy、Kore.ai、Google Dialogflow CX 的官方打断能力；行业主要采用开关/敏感度、最短发言时长或最少词数、忽略附和词、局部不可打断和误打断恢复，并未发现以“连续抢话轮数”作为主流公开配置。

## 2026-09-14 产品与技术差距研究
- 已完成 `docs/语音智能体产品与技术差距研究报告.md`，覆盖电信行业、PolyAI 自动化构建、WebRTC、接口/代码/MCP 工具、Agent Loop、PolyScore、多 Agent、音频原生模型、流程/变量/代码/记忆/RAG/打断和五项算法专题。
- 核心判断：页面入口较完整，主要差距在流程运行时、状态生命周期、统一工具协议、知识治理和语音链路可观测性。
- 下月建议优先级：对话轮次与流程运行时 2.0、变量/实体状态中心、统一工具协议、语音链路与打断 2.0；新增模型和多 Agent 先受控试验。
- 四个流程问题统一以轮次事务、状态快照、输入消费、End 尾路由、作用域自动清除和循环保护解决。

## 2026-09-14 核心模型配置精简
- 机器人基础配置已移除温度、核采样和上下文压缩的前端入口，旧字段继续保留以兼容已有配置。
- “开启思考”调整为“思考模式”；关闭时不展示强度，开启后使用轻度、深度、最大三级中文滑轨。
- 默认思考强度为轻度，并用简短说明明确强度越高通常回答越充分、响应时间也可能增加。
- 根据页面复核进一步压缩布局：大模型类型与思考模式同高并排，思考强度开启后直接在同一输入框内显示三级选项，说明收进提示图标。

## 2026-09-22 计费中心
- 新增独立一级页签「计费中心」，三个视角：用量与余额（还剩多少）、通话消费明细（每一通花了多少）、消费统计（哪台机器人、哪个月花的）。定价与逐通话单的规格见 `docs/计费引擎规格.md`，产品口径见 `docs/计费中心PRD.md`。
- 计费口径：每分钟成本 = 语音识别 + 大模型 + 音色三项分项成本之和；对外单价 = max(0.15 元/分钟, 成本 × 2 向上取整到 0.01 元)。ASR 按每小时单价 × 每分钟 50 秒有效语音、TTS 按每万字符单价 × 每分钟 120 字、大模型按每百万 token 单价 × 每分钟 10000 输入 token 折算。金额 = 向上取整(计费秒数 × 单价厘 ÷ 600)。
- 三个整数刻度分开用：成本存微元（1 元 = 1000000 微元）、单价存厘（1 元 = 1000 厘）、金额存分。全程整数运算，不出现二进制浮点。
- **只有一份账**：`billingData.ts` 只存逐通通话（机器人、时刻、时长、是否接通），月度合计、机器人合计、余额流水、页面上每一张卡片全部由这批通话累加得出。此处推翻第一轮「月度汇总 + 明细样本各存一份」的做法——两份账按各自口径取整，同屏就会出现两个「我花了多少钱」。这条有守卫测试盯着（`tests/billingCenter.static.mjs`）。
- 演示账户：话费 7500 元按最低档 0.15 元/分钟折 5 万分钟，覆盖 7 个月、约 3000 通通话，累计消费与 7500 元余额、流水期末余额三方相等（`RECONCILIATION.ok`）。
- 未计费的通话不显示「0.00 元」，显示留白并由状态列说明原因：「待定价」= 机器人模型没配全、暂不扣费、补齐后补扣；「不计费」= 客户没接通。依据面板对这类通话显示缺哪一项配置，而不是显示占位快照里的 0。
- 发布时冻结一份费率快照；历史通话按各自的快照复算，改配置不影响已产生的费用。发布预览与这份快照共用同一个计算结果，避免「预览一个价、扣费另一个价」。
- 从计费中心跳通话记录时，若通话记录里没有这一通的详情（页面会回落到示例通话），金额整块不显示并说明原因，不允许出现「一通电话配着另一通的金额」。
- 并发按「已购 6 路 / 已分配 5 路 / 还可分配 1 路」呈现；每路在通话开始前按最长 5 分钟预占额度，通话结束按实际费用结算、差额立即释放；余额不足时限制同时进行的路数（页面上叫「被限流次数」）。
- 竞品调研结论（PolyAI、Retell、Vapi、LiveKit、ElevenLabs、Cognigy、Kore.ai、Sierra 及开源方案）记在 README 搜索记录与 `docs/` 内；定价结构借用 QuickVoice 的「版本化费率目录 + 逐项成本明细 + 快照冻结」，额度门禁借用 Dograh 的「通话前校验余额」。
- 已知取舍：成本表沿用需求方口述的三个供应商价格（千问 ASR 0.8 元/小时、MiniMax TTS 2 元/万字符、千问 plus 输入 2 元/百万 token），未采纳调研中查到的公开牌价（如 AWS Transcribe 4.2 元/小时、千问 plus 0.8 元/百万）——换成牌价会让家政回访机器人离开 0.15 元最低档，且与「7500 元 ÷ 0.15 = 5 万分钟」的对外话术对不上。
- 已知限制：通话记录模块只有一通（`4cb67f3a`）有完整详情，其余 7 通跳过去会回落到示例通话、因此不显示计费块。要让 8 通都能看，需要在通话记录侧补齐逐通详情。
- 未改动：成本表 31 条单价、定价规则常量、既有的满意度与监控模块。

## 2026-09-22 计费中心第二轮（审核后修正）
- 上面「计费中心」那条里，「不计费」的说法本轮全部作废：面客一律写「**未接通不计费**」，理由是「不计费」看不出是「没接通所以不收费」还是「平台给免了」，而这两件事对客户完全不同。守卫测试禁止概览、明细、依据面板三个页面再出现光秃秃的「不计费」。
- 逐通通话要拿**自己的一份快照副本**（`captureSnapshotAt` 逐层复制 ASR / TTS / 大模型三块），不能直接引用机器人级那个共享对象。共享引用时改一行配置会连带改掉全部历史通话的单价和金额，而账面上看不出任何痕迹。快照编号 `snapshotId` = 配置指纹 + 时刻，指纹本身**不含时刻**——同一份配置打一万通电话是同一个指纹，这样才能用指纹判断「这两通的价是不是同一套配置算出来的」。
- 待定价原因改读**这通电话打的时候冻结下来的那一份**（`record.billingMissing`），不再回读机器人当前配置。机器人事后补齐了配置也不改这里——否则回头再看这通电话，会用今天已经配好的机器人去解释当时的「算不出价」，客户看到的原因是假的。
- 明细表新增六项筛选（月份 / 机器人 / 计费状态 / 通话类型 / 费率档位 / 关键词）与合计行。合计只累加已计费的通话，未计入的单独报数；平均单价那一格用的是「已计费金额 ÷ 已计费时长」，因为它含了每通向分取整的溢价，**不能**当成逐通费率读，表头里用 title 说明了这一点。
- 消费统计的环比改为与**上月同期**比。本月只统计到 22 日（`CURRENT_MONTH_CUTOFF_DAY`），拿它去比上月整月会得出「消费下降三成」的假结论，而账单其实一分没少。页面上同时给出「上月同期」和「上月整月」两个数。已删除的机器人仍在统计里出现，但打「已删除」标记。
- 未计费的金额一律显示「—」而不是「0.00 元」。依据面板里「0.00 元/分钟」同理——那读起来像「这通免费」。依据面板新增「算不出费率」分支，说明缺什么、并明说**价格补齐后会自动补扣**（不说这句客户会以为这通永远不收费）。
- 依据面板里成本与加价倍率放进默认关闭的「**内部视角**」开关后面。这是给内部评审看算法用的，**客户版上线时这个开关要摘掉**。成本参数里标注 `estimated` 的模型在内部视角里打「演示估值」标记，提醒这些价格是估的。
- 发布价格预览新增变价归因：价格与线上快照不一致时，逐个比对 ASR / 大模型 / 音色 / 音色语言的 displayName，说出「是哪一项改的」；模型组合没变却说变了价，就说明是成本参数或取整规则动了。
- 机器人编辑页的发布预览会读配置里的 `billingRateSnapshot`，而演示数据里的两台机器人本就在计费中心里花掉了几百块。App.tsx 里给这两台机器人补挂了一份发布快照（复制一份、不共用计费侧的对象），否则同一个页面体系里会出现「计费中心说花了几百块、编辑页说还没有生效价格」的矛盾。
- 并发表的预占口径写清楚了：`RESERVE_MINUTES_PER_CALL = 5`，除数 **10 而不是 600**（单位是「分钟 × 厘/分钟 ÷ 10 = 分」，和金额公式不是一个刻度）。原型只做到「算得对、显示得对」，**放行拦截、余额不足限流、预占超时释放这三件事没有实现**——原型没有服务端，拦不住呼叫也发不出告警，要等后端接上。
- 相对原设计删掉了「计费时长」列：演示数据里没有转人工分段，`billableSec === durationSec`，多一列永远是同一个数。取整口径已写进每通快照，将来真出现「转人工后不计费」，按快照口径就能把新老账分开复算。
- 页面文案大扫一遍内部术语：`预估冻结金额`、`今日被限流`、`加价`、`快照编号`、`token`、`成本价` 六个词不许出现在任何面客页面（用量与余额、通话消费明细、消费统计、计费中心首页、发布价格预览、通话记录详情），守卫测试逐文件扫描。
- 顺带修了两处演示数据自相矛盾：示例通话的结束时间改为开始时间 + 3 分 49 秒，录音分段从两段各 3 分 50 秒（合计比整通电话还长）改为振铃 4 秒 + 112 秒 + 113 秒 = 229 秒。
- 文档：`docs/计费中心PRD.md` 头部加了口径修订说明与一份权威锚点数字表（余额、折算分钟、通话数、各期消费、并发、示例通话），正文里所有演示数字与之一一对齐；§10 补记了实现期间对 Q3 / Q7 / Q10 / Q17 的落定与偏离，并新增 Q19「账户余额要不要在机器人之间分摊」（P0 决定：不分摊，余额只出现在账户层）。`docs/计费引擎规格.md` 的并发预占一条改为已实现的口径。
- 机器人编辑页新增「当前配置单价」只读条（`components/billing/BotRatePreviewBar.tsx`），挂在核心模型配置区的最后一行，改大模型 / 音色 / TTS / ASR 任一项立刻重算，不用保存。**单价不在这个组件里算**：机器人表单算一次后作为 `rateResult` 传下来，与发布弹窗共用同一个结果，编辑页自己再算一遍就会出现「编辑页一个价、发布弹窗另一个价」。折算分钟数用套餐话费总额，不硬编码 7500。算不出价时整条换成「当前配置暂无价格」，既不显示 0.00 元/分钟，也不给「查看计费依据」入口。
- PRD 里几处写的字段名与落法本轮改为按实现记录，并说明为什么改：`BotBillingRateSnapshot` → 实际是 `BotRateSnapshot`（会被逐通快照复用，不该带「Bot」限定词）；通话详情没有挂 `billing?: CallBillingRecord` 字段，而是用 `callId` 反查（挂字段等于把计费记录复制一份，两边会各自演化；反查还能顺带判断「这通到底是不是这一通」）。§7.8 另加一张「实现上屏的文案」对照表，写清设计稿里哪些条目没做（E1 / E4 / E6 / A5 / L1）、哪些落成了别的样子（E2 / E3 / A1 / A2）。
- 侧边栏没有把「计费中心」插进「监控报表」分组，而是新开分组「账户与计费」放在末尾：放进报表分组会让人以为它也是报表的一种，而且后续「充值 / 发票 / 套餐管理」有地方放。路由 `<BillingCenter />` 不接收 `bots`——计费侧的机器人档案自带费率和状态，与页面里的演示机器人配置是两个数据集，接进去会变成两处定义。这些偏离都记在 PRD §7.1。

## 2026-09-22 计费中心第三轮（终版 · 审核后收口）
- **计费算术第一次被真的跑起来**：新增 `tests/billingCenter.money.mjs`（39607 项断言）。之前的 `billingCenter.static.mjs` 全是「常量相等 / 字符串存在 / 正则匹配」，它拦得住「有人删了向上取整这个词」，拦不住「有人把 `Math.ceil` 改成 `Math.round`」——后者会让全站每一笔金额都变，而静态断言全绿。新测试用 esbuild 把 TS 打成内存里的 ESM、经 data: URL 导入（不落临时文件、不依赖构建产物），逐通复算演示数据里的每一笔金额。**做了一次变异验证**：把 `ceilDiv` 里的 `Math.ceil` 改成 `Math.round`，新测试红、旧测试仍绿——正是要拦的那件事。
- 顺带补实了静态测试里三处**假守卫**：① 套餐金额检查原先只匹配 `\b7500\b`，而页面上写的是「7,500」（带千分位逗号），这条断言恒真——现在先去掉逗号再匹配；② `ceilDiv` 只检查函数名存在，不检查函数体里是不是 `Math.ceil`；③ 「内部视角」那条断言的四个字在文件注释里也有，把开关标签改成任何东西都还是绿的——现在断言的是客户看得见的那个标签「平台内部视图」。另加一条 `computeReserveCents` 除数必须是 10（不能抄成 600）的守卫。
- **消费口径统一（本轮最重要的一处修正）**：`MONTHLY_USAGE` 的通数、时长、金额三列改为**只累加已计费的通话**。原先是「全额算通数、只算已计费的钱」，页面上的后果是客户拿 ¥195.77 除以 812 分钟，得到的单价比这台机器人实际收的价低；而同一个「消费明细」表尾对同一批数据写的是 810 分钟——同一屏两个数。表头相应改为「已计费通话数 / 已计费时长 / 已计费金额」，表下说明待定价和未接通的通话为什么没算进来。这条已写进规格文档 `计费引擎规格.md` §5.14，并由算术测试直接断言「统计页本月行的通数/时长 = 明细页本月已计费行的通数/时长」。
- **幂等键必须标明是哪一件账务事实**：未接通记 `:free:1`、待定价记 `:pending:1`，`:billed:1` 只留给真正扣钱的那一条。原先三条分支共用一个 `:billed:1`——待定价的通话要补扣时，扣费请求会因为「这个键已经用过」被当成重复请求丢掉，客户永远收不到这笔补扣，而账面上看不出来。同时原因码分开：待定价记 `MODEL_NOT_PRICED`（新枚举值），不再顶替 `NORMAL`——结算侧是按原因码挑补扣对象的。测试锁了「记录与对应流水的键一致」「键在整个集合内唯一」「非已计费记录不得占用 `:billed:1`」。
- **演示通话的 Call ID 重做**：原来生成的叫 `gen_2026-03_bot_home_visit_0001`，明细表只显示前 8 位，于是整列都是「gen_2026」——客户想报一个单号来问都指不清是哪一通，搜索输一次命中所有行，还把内部命名暴露在面客页面上。现在生成 8-4-4-4-12 形状的十六进制串（独立随机流，不碰生成时长/号码的那条流，否则全站锚点金额都会被洗一遍）。原先共用 `ce1d7a55` 前缀的 7 通本月通话也拆成互不相同的 ID。测试锁「显示用的前 8 位不重复」。
- 面客文案逐句过了一遍，改了 12 处，每一处都对应一个客户会误解的点（完整对照表在 `docs/计费中心PRD.md` §7.8.2）：余额不足的提示从「说并发」改成「说余额扣完之后会怎样」；「不计费」一律改「未接通不计费」；通话记录详情里「不收费」改「未接通不计费」、「计费单价」改「适用费率」且未接通的通话不再渲染出「0.00 元/分钟」（那是占位快照里的 0）；明细表「本筛选范围」不再列恒为 0 的类别；号码搜索两侧都去掉脱敏星号（客户照 CRM 的完整号码搜原先永远搜不到）。
- 一致性收口：`STATUS_META` 原来在明细表和依据面板里各写了一份，现在统一由 `billingUi.tsx` 的 `BILLING_STATUS_META` 导出——两处各写一份正是 `CallRecordDetail` 里漂出那句「不收费」的原因。依据面板自己兜底 `pending`，将来任何新调用方都不会把占位快照渲染成真价格。
- 可达性：依据面板打开时把焦点移到关闭按钮、关闭时还给原来的元素；页签补 `role="tablist" / tab / tabpanel` 与 `aria-selected`；明细表可点行的尾部箭头标 `aria-hidden`（它是装饰性的，正文里另写明「或行尾的『为什么这个价』按钮」这条键盘可达路径）。
- 依据面板和单价条改为**条件挂载**（不再传 `null`）：面板里「平台内部视图」是组件内部状态，常驻树上会让下一次打开沿用上一次的选择——客户勾过一次成本明细，之后每次点开都会直接看到那一块。同时给静态测试补了一条：编辑页单价条里不许出现套餐金额的字面数字（应从 `BILLING_PACKAGES` 取），否则运营改了套餐，同一句话里的两个数会互相矛盾。
- 记在文档里、**没有改代码**的三项（都是原型范围内不该动的）：① `computed_at` 写的是通话开始时刻，与规格里「`computed_at ≥ ended_at`」的审计要求不符——原型没有异步结算链路，这个字段实际被当作「记在哪个月」的归属依据用，已在界面把字段名改成「通话时刻」名实相符，接后端时必须拆成两个字段（规格 §5.9 记了偏离与改法）；② `语言 || 'zh-CN'` 的回退保留——去掉它会让所有没填主语言的机器人变成「待定价」，这是存量配置的现实约束；③ 明细表可点行没有加 `tabIndex`——行内「为什么这个价」按钮已经是同一条路径的键盘可达版本，再给每行加一个 tab 停靠点只会让 Tab 键在同一行上停两次。

## 2026-09-23 计费中心第四轮 · 阶段一（余额改批次制）
- **余额不再是一个池子，改成一笔一笔的额度批次**（`FundGrant` / `FundAllocation` / `GrantBalance`）。起因是「哪笔钱什么时候到期」这个客户一定会问的问题，一个总额答不上来。资金分三种性质 `FundKind = voucher | cash | credit`：`voucher` 是随套餐给的映射额度、有有效期，`cash` 是客户自己充的钱、无限期，`credit` 是平台授信、自有资金用尽后兜底且一旦动用就是欠款。充值方式分两类 `RechargeSource = auto | manual`（系统自动充值走代金券体系、有有效期；单独充值无限期）。
- **账户级仍然是单一连续余额链**：跨批次的拆分记在 `LedgerEntry.allocations` 里，**不**拆成多条 `call_charge` 流水。因此既有两条口径原样成立——`isLedgerChainIntact()` 继续通过，「每通电话恰好一条扣费流水、幂等键等于记录自己的键」不变。`LedgerEntry` 新增必填的 `fundKind`，`LedgerEntryType` 新增 `expire`（批次到期清零，金额为负，每批次各一条）。
- **扣费顺序：先扣快到期的**。`CONSUME_ORDER = ['voucher', 'cash', 'credit']`，同一性质内按到期日升序、无限期（`expiresAt === null`）排最后，由 `compareGrants` 一处实现；页面调 `grantsByConsumeOrder()` 拿顺序，自己不再排一遍——两处各排一次迟早给出不同的「先扣哪笔」。分摊用 `allocateAcrossGrants`，额度不够时**当场抛错**而不是默默记一笔扣不动的账：演示数据里走不到这一步，走到就说明余额与流水已经对不上。
- **有效期按套餐周期**：2026-03-01 生效 → **2027-03-01 00:00 清零**，不结转下一期。到期结算是自动的（`settleExpiries` 在每次账务变动前跑一次、数据截止日再跑一次），生成幂等键为 `{grantId}:expire:1` 的 `expire` 流水；2027-03-01 之前的演示数据因此不会出现任何 `expire` 条目，之后的批次仍会被自动清零。
- **演示口径改由「路数 × 单路」推导**：6 路并发 = 套餐总额 **6 万元**（6 × 1 万），其中映射额度 **4.5 万元**（6 × 7500 = 4500000 分，按最低档 0.15 元/分钟折 30 万分钟），另 **1.5 万元**是平台与并发服务费——客户确实花了，但它不进余额、不能打电话。余额 **42,689.74 元**（4268974 分）、累计已用 **2,310.26 元**不变。守卫测试的锚点块同步改为全部从 `PURCHASED_CONCURRENCY` 推出，客户加买 1 路只改路数，额度与套餐总额跟着走。
- **折算分钟数从面客的余额与汇总页下线**：单价是动态的，今天折出 28 万分钟、明天调价变 19 万，客户会拿第一次看到的数去对。余额、已用、汇总一律只给金额口径。唯一保留折算的地方是机器人编辑页的单价条——那个分钟数就写在这个单价旁边，答案与依据同一行，不会产生误解。守卫测试改为断言三个折算常量不出现在「余额与额度」页和计费中心首页。
- **不再说「话费」**：余额是语音智能体的余额，不是话费（话费是运营商的概念，客户会以为要充到手机号里）。守卫测试按复合词拦——`话费余额` / `含话费` / `剩余话费` / `已用话费` / `话费明细` / `套餐话费`；「不是话费」这类澄清句正是要说清这件事，不拦。
- **页面**：页头删掉标题与那句「这里能看到每一笔钱是怎么花出去的」，只留时间范围（今日 / 昨日 / 本周 / 本月 / 上月 / 自定义）与截止日，标题由面包屑承担。「今天」取数据截止日而不是 `new Date()`，否则换一天打开，「本月」会变成一个演示数据里没有的月份。概览四卡改为：**余额**（标「截至今日」，固定读数据截止那一刻，**不跟筛选走**——筛选变的是这段时间花了多少，不是现在还剩多少）、**可用额度**（账面余额扣掉通话中预占；未开通授信时明说）、**本期消费**（通话 N 通，其中智呼 M 通）、**本期充值**（系统自动充值 vs 单独充值分开报）。删掉「剩余可用时长」与「累计通话」两张卡。
- **「用量与余额」页改名「余额与额度」**，回答的是「还剩多少、哪笔什么时候到期」，不再回答「花了多少」——后者在资金流水与消费统计里。新增四块：余额构成（代金券额度 / 单独充值 / 信用额度 / 冻结中 / 欠费 + 账面余额与可用额度两行合计）、额度批次明细（来源 / 到账 / 原始 / 剩余 / 有效期至 / 状态，含 30 天内到期的醒目提示）、套餐（映射额度与平台·并发服务费分列，说明余额永远小于套餐金额不是算错）、预警设置（**金额阈值**——「余额低于这个数就提醒」，不用「还能用几天」：那要预测消耗率，与「价格是动态的」自相矛盾；渠道邮箱 + 短信，各带接收人）。
- 本阶段只做「钱怎么分、什么时候到期、页面上看得见」。**未做**：资金流水页（充值记录 / 扣费记录）、通知记录页、信用额度开通与欠费提醒的真实发出、报表与导出、以及在途通话的额度用尽处理——按计划都排在阶段二 / 阶段三。预警设置的阈值与渠道目前只落在页面状态里，位置先占住（客户找不到「在哪设提醒」就等于没有这个功能），发出与留档等通知记录那部分接上。
- 演示数据里充值只有一笔（2026-03-01 套餐映射额度 4.5 万元），因此「本期充值」在任何区间都是 ¥0.00 并写明「这段时间没有充值到账」——这是实数，不为了让卡片好看而编一笔充值。

## 2026-09-23 计费中心第五轮 · 阶段二（资金流水 + 智呼口径）
- **新增「资金流水」页签**（`components/billing/FundFlow.tsx`），三个子页签：**全部**（7 列）、**充值记录**（8 列）、**扣费记录**（9 列）。它是同一本账的三种视角，取数只走数据层的 `fundFlowInRange(from, to, type?)`，页面不另起一套。演示数据里「全部」是 3023 条（1 充值 + 3020 通话扣费 + 1 通话前预占 + 1 预占释放），默认倒序、分页 20/50/100 可选。
- **每一行都带期初余额与期末余额**，且**期初由「期末 − 这笔金额」反推**，不另存一份：存两份迟早各走各的，而页面上看不出哪份是错的。预占与释放虽然只是把钱占住又还回来，**也留在「全部」里**并各给中文名（「通话前预占」「预占释放」）——把它们藏起来的话，下一行的期初会和上一行的期末对不上，客户会以为漏了一笔。测试锁「相邻两行首尾相接」，且**筛选之后仍然相接**（本月第一条的期初必须接上 8 月最后一笔的期末，不能从 0 重新开始）。
- **跨批次的扣费会标出「含 N 个批次」**，并写明这笔钱扣的第一段来自哪个批次。只说第一段，客户按批次名去核对会发现金额对不上，而账本身没错。
- **页面级时间范围只贯通「概览四卡」与「资金流水」**。通话消费明细保留自己的月份筛选（默认本月，客户打开计费中心最先想知道的就是这个月花了多少）、消费统计保留自己的全部 / 最近 3 个月 / 最近 1 个月——它们各自的默认口径与页面级筛选要回答的问题不是同一个，硬接上去会让「消费明细默认本月」这条既有口径失效。
- **「通话 N 通（其中智呼 M 通）」贯通三处**：概览卡「本期消费」、消费统计的按机器人表与按月表、余额与额度页的「费用怎么算」结语。客户问的是「多少通通话」和「多少智呼的时长」两件事，只给合计等于没回答。口径与通话总量完全一致（**全量含未接通与待定价**，按月的只算已计费），所以客户拿两个数相减不会减出负数；测试锁「智呼通数与时长都 ≤ 合计」「按月的智呼 ≤ 当月已计费通话数」「按月之和 ≤ 全量智呼」「区间用量由逐通判定得出」。
- **守卫测试新增第二十节**（`tests/billingCenter.money.mjs`，断言数 60779 → **79194**）：区间筛选真的按日期截断（不是点了没反应）、扣费流水条数 = 已计费通数且能指回那一通、期初反推与首尾相接、**分摊之和等于这笔金额的绝对值**（分摊永远记正数，流水金额带方向，比符号会把每一笔扣费都判成错的）、批次余额与账户余额不分叉、概览卡的「本期充值」与充值记录页合计是同一套数。静态测试第十二节同步锁住充值记录 / 扣费记录 / 期初余额 / 期末余额 / 通话前预占 / 预占释放 / 其中智呼这些面客字样的存在。
- 本阶段**未做**：通知记录页、信用额度开通与欠费提醒的真实发出、报表与导出（按计划都在阶段三）。预警设置的阈值与渠道仍只落在页面状态里，但右上角已改为「每次提醒都会留档，可回查」，不再指向一个还不存在的页签。

## 2026-09-23 计费中心第六轮 · 阶段三（信用额度 + 欠费提醒 + 报表导出）
- **新增信用额度（平台授信）**：`types.ts` 加 `CreditLine`（总额度 / 开通时间 / 有效期 / 状态），演示口径为 **¥10,000、2026-03-01 开通、2027-03-01 到期、active**。授信是「能欠多少钱」，**不是一个钱包**：开通授信不产生到账流水，也不让账面余额变多。因此三条口径同时改：**账面余额 = 代金券额度 + 单独充值**（不含授信）、**可用额度 = 账面余额 + 可用授信 − 通话中预占**、**欠费 = 已动用的授信**。把授信算进余额，客户会以为账上凭空多了一笔钱，而「余额不足」的提示再也不会出现——那正是他最需要被提醒的时候。
- **透支上限取代「余额永不为负」**：阶段零定的「每条流水与每条记录的 `balanceAfterCents >= 0`」被本阶段**推翻**。那条规则写在还没有授信的时候，当时余额穿负就意味着扣了一笔根本没有的钱；现在余额用尽后会接着动用授信垫付，余额链本来就会走负，负的那一段就是欠费。算术测试第十四节的断言相应升级为「透支不得超过授信额度」——少了这条，一笔超出授信的钱能被安安静静地记成欠费，客户在页面上看不出平台多垫了多少。
- **授信不是批次**：它不进 `BILLING_GRANT_BALANCES`，`applyAllocations` 遇到 `CREDIT_GRANT_ID` 直接跳过（没有批次余额可动），欠费由 `creditUsedAt(asOf)` 从流水现推而不是另存一个字段——还款、冲正、退款都动这个数，多存一份就多一处分叉的机会。`allocateAcrossGrants` 新增第三个参数 `creditAvailableCents`：自有批次用尽后追加一段 `CREDIT_GRANT_ID` 分摊，**仍不足才抛错**；自有资金够用时给再多授信也不动它（先欠钱再花自己的钱是客户绝不能接受的顺序）。演示数据不走授信路径，所以**全部既有锚点不变**（余额 ¥42,689.74、累计已用 ¥2,310.26）；把 `CREDIT_LINE.status` 改成 `'closed'` 即可演示「未开通」。
- **欠费提醒三档**：`threshold`（账面余额低于预警值，可在页面改）/ `exhausted`（余额已用尽）/ `debt`（已经欠费）。**后两档 `configurable: false`——它们是事实不是偏好**，都能关的话客户可以关掉「已经欠费」的提醒。渠道邮箱 + 短信（`NOTIFY_SETTINGS.channels`），频率为每档每天最多 1 次、最多连发 3 天，三档各自计数（「快用完了」提醒过之后，真的用尽时还会再提醒一次——这两件事对客户不是一件事）。提醒只是通知，**不会自动充值**（那是没有授权的扣款动作），这句话写在通知记录页上。
- **新增「通知记录」页签**（`components/billing/NotifyRecords.tsx`）：提醒规则表 + 提醒文案预览（`NOTIFY_TEMPLATE`，用占位符而**不是**写死演示余额——写死了，真实触发时短信里会带着一个别人的余额发给客户）+ 提醒记录表（记录号 / 发送时间 / 档位 / 触发时余额 / 触发时欠费 / 方式 / 接收人 / 发送结果）。**记录长期保留、不随页头的时间范围筛选**——它是对账凭证，点一下筛选就查不到发过的提醒，客户说没收到时平台就答不上来。短信文案不带链接：账务类短信带链接容易被当成诈骗。
- **提醒记录从空数组起步**（`NOTIFY_RECORDS = []`）：截至 2026-09-22 余额没有低于过预警值、也没有用尽或欠费，三档都没触发过。**不为让页面好看而编几条记录**——编出来的提醒记录是一条伪造的账务事实，客户会拿它当凭证。页面用空态说明这一点，并靠「提醒文案」那块回答「提醒长什么样」。
- **新增「报表导出」页签**（`components/billing/ReportExport.tsx` + `billingReports.ts` + `billingCsv.ts`）：**五张报表**——充值记录（8 字段）、扣费记录（9 字段）、通话消费明细（9 字段）、消费统计（9 字段）、提醒记录（9 字段）。前四张是账，第五张是凭证。每一列都写清单位、这是什么、怎么算出来的，并给出**本范围内第一行的真实示例**（示例取真实数据而不是手写：手写的示例改了字段忘了改，这份口径表就成了一份假文档，而客户是照着它去对账的）。
- **导出与页面同源**：报表的每一行都来自与页面完全相同的那几个函数（`fundFlowInRange` / `BILLING_CALL_ROWS` / `MONTHLY_USAGE`），标签也从 `billingUi` 取——页面上叫「通话前预占」，导出的文件里也必须是「通话前预占」。导出自己再查一遍库、再算一遍，屏幕上和文件里迟早不一样，而客户是拿着文件来对账的。算术测试锁「导出的充值/扣费记录行数 = 页面上的行数」。
- **CSV 三条口径写在 `billingCsv.ts` 里而不是靠调用方记得**：① 带 BOM、行尾 CRLF（不带 BOM 的中文 CSV 用 Excel 打开是乱码，只给 LF 则在记事本里挤成一行）；② 金额导出成纯数字 `42689.74`、不带千分位与货币符号（带逗号的数字在 CSV 里会被切成两列），时长统一导出成分钟保留两位小数；③ 公式注入防护——以 `= + @` 开头的单元格加前置单引号。**本阶段修掉了这里的一处真实缺口**：原判据是正则 `^-(?!\d)`，注释写着「`-1+1` 这种也是公式」，可 `-` 后面跟的正是数字，`-1+1` 恰好能溜过去；改成判「整个值是不是一个合法的负数」，负数金额原样保留（加了撇号 Excel 就当文本，一列金额求和全是 0），`-1+1` 这类则被拦下。
- **单次导出上限 3 个月**，超过就拦在下载之前并给出提示（上限不是技术限制是业务限制：一次导三个月以上，客户多半想要一份「历史全量」，那该走定时的账期报表）。**每次导出留一条记录**（记录号 / 导出时刻 / 报表 / 时间范围 / 行数 / 字段数 / 操作人 / 文件名）——账务数据出域必须有痕，否则「谁把客户数据导走了」永远答不上来。原型不落库，记录只在**本次会话内真实追加**；导出时刻用**真实时间**（导出是客户此刻做的事，与页面别处以数据截止日当「今天」不矛盾）。
- **未接通与待定价的通话在导出的「通话时长 / 适用费率 / 本通费用」三列留空，不写 0**：写 0 会被读成「这通免费」，而实际是「暂不扣费、定价后补扣」。待定价的通话**时长照常导出**——电话真的发生了，只是价格还没定。
- **计费中心扩到六个页签**：余额与额度 / 资金流水 / 通话消费明细 / 消费统计 / 通知记录 / 报表导出。低余额横幅按有无可用授信分叉：有授信时说明「余额扣完后会先动用信用额度垫付（当前可用 ¥X），垫付的部分形成欠费，欠费未结清时新的通话将无法发起」，无授信时只说新通话无法发起。**低余额判定看的是自有资金**（账面余额）而不是可用额度——把授信算进来，余额早就见底了这条提示却一直不出现。
- **守卫测试**：算术测试 79194 → **85313** 项（新增第二十一节：授信只在自有资金用尽后动用且只垫差额、授信不够时抛错、没开通授信时自有不足也抛错、授信不进批次列表、授信到期后不能再透支但已欠的仍要还、CSV 的 BOM/CRLF/逗号/引号/换行/公式前缀六种转义、负数金额原样保留、月跨度算法、五张报表的字段数与数据列数一一对应、三种通话状态各自的空值口径、提醒记录不随范围变、口径表示例来自真实数据）。静态测试新增第十三节（授信三条恒等式、授信垫在自有资金之后、三档提醒与两种渠道、提醒记录空数组起步、报表清单与字段口径、导出必须走 `billingCsv` 且页面不许自己拼 CSV、导出记录留下时刻与操作人、六个页签都真的挂上了面板），并把 `NotifyRecords` / `ReportExport` 登记进第七节「界面零件只有一份」与第十节「面客页面不出现内部术语」两个循环。新断言用变异测试验证过确实会失败（把授信垫付搬到自有批次之前、把授信算进账面余额、编一条假的提醒记录，三种改法都被拦下）。
- **文档**：PRD `docs/计费中心PRD.md` 与规格 `docs/计费引擎规格.md` 在阶段三统一补齐授信、提醒、导出三块（阶段一 / 阶段二欠的账一次还清）。
- 本阶段**未做**：提醒的真实发出（原型无服务端，提醒记录没有真实触发源）、自动充值（按计划附录 A #7 本期不做）、导出的审批与水印（#8 按「按角色控制 + 记审计」落地）。

## 2026-09-23 计费中心第七轮 · PRD 与文档收口
- **PRD 从 1065 行改到 1364 行，按六个页签重排**：新增 §7.3 页签总览（并写明页面顶部的时间范围只贯通「概览四卡 / 资金流水 / 报表导出」三处，消费明细与消费统计保留各自默认口径）、§7.5 资金流水、§7.8 通知记录、§7.9 报表导出；原 §7.4–§7.8 整体后移为 §7.6–§7.12。§7.4 由「用量与余额」改名为「余额与额度」并重排成七个区块（余额构成 / 信用额度 / 额度批次明细 / 套餐 / 预警设置 / 并发容量 / 费用怎么算）。
- **§8 的 P0 从 R1–R10 扩到 R1–R15**：新增 R11 信用额度、R12 欠费与余额提醒、R13 资金流水页、R14 通知记录页、R15 报表导出页；R3/R4/R5/R7/R10 按已落地的实现重写（例如 R5 的「计费时长」列本期没有单独做、R7 不再有机器人余额表——这两条都写成了「做不到 / 不做了」而不是留着当承诺）。P1 收敛为 R16–R20、P2 后移为 R21–R27，并在 P1 下写明三项收回（导出与余额预警升级成 P0、按「还能打多久」排序随机器人余额表一并删除）。**加条目时先整体后移再补编号**：只加 5 条不动后面，P1 的 R19–R21 会和 P2 撞号，而引用数字散落在 Q2/Q6/Q9 里。
- **三条口径写进需求正文而不是散在实现里**：① 账面余额不含授信、可用额度含可用授信（两条恒等式 R4 与 R11 各写一遍，页面与测试同源）；② 折算分钟数从面客口径下线，唯一保留在机器人编辑页的单价条（R4 与 R8 各写一次「不出现」）；③ 提醒记录与导出记录是对账凭证，**不随页面时间范围变化**（R12/R14/R15）。
- **改写手法**：1300 行中文 PRD 用「行锚点 + 计数校验」的 Python 脚本分三刀改（`prd_s7.py` / `prd_s8.py` / `prd_s8b.py`），每处替换前断言全文唯一匹配，不唯一就中止且不落盘；改前备份了原文。不用 sed/awk（中文匹配会坏），也不用长串精确匹配（一处半角括号就能让整块对不上）。
- **README 只补两条重要设计决定**（其余细节留 PRD）：**授信口径**——账面余额 = 代金券额度 + 单独充值（不含授信）、可用额度 = 账面余额 + 可用授信 − 通话中预占，开通授信不产生到账流水；**导出留痕**——五张报表、单次上限 3 个月、每次导出留一条含导出时刻与操作人的记录（账务数据出域必须有痕）。另把「三个子页签」那句改成六个页签，并补记钱按批次管理、套餐金额与余额不是同一个数。README 的检索记录里写明：第四至第六轮**未新增外部检索**，沿用第一轮已对照的项目结构（CGRateS 的余额有效期、Dograh 的额度门禁、Lago 的信用额度与账单导出）。

## 2026-09-23 计费中心第八轮 · 竞品调研与客户动作入口
- **本轮先调研再设计**。此前六轮的设计是照着「我们觉得账务应该长这样」做的，没有一条是「行业怎么做」。本轮补上：四组共 42 家，逐家只写**实际打开过的页面**，推断必须给依据并标明，查不到就写查不到。落成 `docs/计费中心竞品调研.md`（§0 取证口径 / §1 对象表 / §2 海外语音 AI 11 家 / §3 专业计费平台 12 家 / §4 云与通信 11 家 / §5 国内同行与国内云 8 家 / §6 十条共识 / §7 差距清单）。原始取证归档在会话临时目录 `research/`（不入库）。
- **最重要的发现是「国内同行查不到」**：容联七陌帮助中心 147 篇文档全量过滤 + 站内逐词搜索，话费 / 余额 / 充值 / 账单 / 欠费 / 消费明细 / 预扣 / 冻结**全部返回「未找到相关文档」**；天润融通 279 篇同样没有一篇涉及计费，且话单字段页是图片表格；两家官网都没有价格页。智齿 / 网易七鱼 / Udesk / 讯鸟 / 中关村科金未能取证。**结论：国内这一层计费与价格一样是合同里谈的、不在文档里写**，所以「客户会拿同行来比」这个前提不成立——客户真正拿来对照的是自己天天在用的那朵云的费用中心。重心因此移到腾讯 / 阿里 / 火山 / 华为 / 百度 / 京东六家。
- **国内云这一组给出了三条可直接对标的锚**：① 额度必须拆批次、抵扣顺序「先到期的先用」——腾讯 / 阿里 / 火山 / 华为四家口径一致无一例外，我们的 `CONSUME_ORDER` + FEFO 完全对齐；② 提醒频率「每天 1 次、最多连续 N 天」是行业标准写法（腾讯 5 / 阿里 5 / **华为 3**），我们的「1 次 / 3 天」与华为逐字一致；③ **预扣在国内有专名**——腾讯叫「冻结 / 解冻」、阿里叫「虚扣 / 实扣」、火山叫「预留金额」、华为叫 `unRecordBillEstimate`，我们页面上用的「冻结中」与腾讯的正式叫法一致，**不要改成「预授权」这种海外词**。
- **修掉一处真实的纰漏（本轮最有价值的一条）**：`BillingCenter.tsx` 的低余额告警条按「映射额度总额的 20%」（= ¥9,000）判定，而「预警设置」里客户设的是**绝对金额 ¥5,000**——同一屏上两个对不上的数，而这条正好是「余额快用完」唯一的出口。改成 `ALERT_BELOW_CENTS = NOTIFY_SETTINGS.thresholdCents`，页面告警条与预警设置读同一个值。删掉 `LOW_BALANCE_RATIO`。选绝对金额而不是百分比（火山用百分比）：客户能理解「低于 5000 就提醒我」，理解不了「低于 20%」——百分比的分母还会随加购变化。
- **必改项 ①：补全部客户动作入口**（此前计费中心「只能看不能动」）。新增 `components/billing/RechargePanel.tsx`（充值：面额档 + 自定义、三种方式**各自写明到账时间**、提交后说明「到账前余额不变」与「到账后作为新批次、有效期无限期」；自动充值三件套「低于 / 充到 / 每月最多」+ 地板余额 + 每 24 小时次数上限不可解除，校验在客户填错那一刻就说清，不等到保存后报错）与 `components/billing/PackageActionsPanel.tsx`（续费顺延主套餐到期日、加购并发**自到账起另算一年**、发票可开票金额 = 已支付 − 已开票）。**这一条同时消掉一处自相矛盾**：提醒文案里写着「充值到账后即可继续发起通话」，而页面上原本无处可充——Synthflow 犯过同样的错，文案承诺一件产品做不到的事比不承诺更糟。自动充值**只能绑支持代扣的方式**（对公转账没有代扣授权，标 `autoRecharge: false` 并写明原因），充值默认关闭（会自己花钱的开关不该替客户默认打开）。
- **必改项 ②：补到期提醒**。`AlertTier` 加第四档 `expiry`，`EXPIRY_TIER_DAYS` = 30 天（可选、默认关）/ 7 天 / 1 天（默认开）。档位这么定是有意的：7/1 与百度智能云对「资源包到期」的 7/3/1 同一量级，站得住「国内有同行这么做」；30 天在 34 家竞品里**没有任何一家给到**，做成默认会比全行业都激进，所以留成开关。**不要写成「对标行业惯例」**——余额类提醒没有「提前 N 天」这一说，这是我们的加法（理由是我们的映射额度一年一清零、金额到万级，不做等于让客户白损失）。`NotifyRecord` 加 `daysLeft` / `grantTitle` 两个可选字段：只记「发过到期提醒」，事后没法回答「是提前 7 天还是提前 1 天发的」，而那正是客户会追问的。
- **必改项 ③：提醒文案补第三句**。原来是「提醒只是通知，不会自动充值」，现在补齐三句：**不会自动充值**（充值必须你确认）／**不会中断正在进行的通话、不会删数据**／**但余额和授信都用尽后新通话会被拦住**。第三句最容易被漏掉：GCP / Azure / 阿里云的原文是「告警不会停止你的用量」，那是因为它们**后付费**；我们是预付制，余额用尽新通话确实发不出去，照抄那句话等于把「会拦住」这件事藏起来。到期提醒文案单独一套（`NOTIFY_TEMPLATE.expiryEmailBody` / `expirySmsBody`）：客户看到「余额 42689 元」不会动，看到「有 15000 元将在 7 天后清零」才会动，两个事由不能共用一套文案。
- **数据层新增**（`billingData.ts`）：`RECHARGE_METHODS` / `RECHARGE_PRESET_CENTS` / `RECHARGE_MIN_CENTS`（¥1,000）/ `AUTO_RECHARGE_LIMITS` / `AUTO_RECHARGE_SETTINGS` / `INVOICE_PROFILE` + `invoiceableCents()` / `concurrencyQuote(ways)` / `renewalQuote()` / `nearestExpiry(asOf)`。**`nearestExpiry` 抽出来是为了让「还有几天到期」只算一次**——余额与额度页、通知记录页都要说这句话，各算一遍会算出两个天数（原 `UsageOverview` 里的 `daysBetween` 已删）。自动充值的默认阈值直接取 `NOTIFY_SETTINGS.thresholdCents`：两件事本来就同一时刻发生，默认给两个不同的数会让客户先怀疑其中一个写错了；但**两个值仍各自可改**（一个只通知、一个会花钱，合并成一个开关是不负责任的）。
- **验证**：算术测试 **85313 项通过 0 失败**（锚点未变：余额 ¥42,689.74、累计已用 ¥2,310.26）；21 个静态测试逐文件跑**全部通过**（`safetyHitlException.behavior.mjs` 的既有失败不计）；`npx tsc --noEmit` **88 行，与基线一致，`components/billing/` 下 0 错误**；`node scripts/check-voice-agent-features.cjs` **27 PASS 0 失败**；`npm run build:app` 通过。
- **守卫测试补上第十四节**（第八轮收尾，把本轮所有新口径锁住）：14.1 充值方式的可代扣标记（对公转账与客经代充必须 `autoRecharge: false`、在线支付必须 `true`、每条必须写到账时间、自动充值的扣款方式必须是支持代扣的那一个、页面上要写明「只有在线支付能代扣」）；14.2 自动充值三件套与两条硬约束（阈值与目标的差值必须校验、次数上限**不能做成可编辑输入框**、默认必须关闭）；14.3 到期提醒四档（30 天可选默认关、7/1 默认开、`expiry` 档不可关闭、**可关闭的档位有且只有一档**）；14.4 提醒文案三句话 + 到期文案不共用 + 空值字段必须被渲染；14.5 承诺与能力对齐（`UsageOverview` 必须挂上充值面板与套餐动作面板、必须有「立即充值」入口）；14.6 报价必须现算（三个报价函数都要被调用、页面里不许出现写死的年份——**只看代码不看注释**，注释里举日期例子是说明、写进 JSX 才是错）；14.7 页头告警线与预警设置同源（必须读 `NOTIFY_SETTINGS.thresholdCents`，且不许再出现百分比口径）。
  **这一节锁的是「承诺与能力必须对得上」**：提醒文案写「请及时充值」而页面上无处可充、下拉里出现不支持代扣的方式——这类不一致在代码里看不出来，只有把两处放在一起比才会暴露，而客户撞上它的方式恰好是最难受的那种（按提示去找，找不到）。
- **调研文档按全量文档核对做了两轮更正**（Bland / Synthflow / Voiceflow 三家官方文档全量下载后核对，Bland 64 页 + Synthflow 255 页 + Voiceflow 449 页）。**更正的价值在于记住了「早期只翻营销页会得出什么错结论」**，所以旧结论以行文保留、不改写成「一开始就对」：① §2.4 的「三家都没有低余额主动推送」→ 实为**两家有、一家含糊**（Voiceflow 明文「接近额度时你会收到通知」，渠道补为**组织管理员邮件**）；② 「额度有效期只有 Synthflow 有明文」→ **Voiceflow 也有明文且取向相反**（额度在订阅周期末过期、月付不逐月结转、**年付一次性发放全年额度**），只剩 Bland 一家空白；③ 「通话前预占查不到」→ 拿到平台自己的**明文反证**（Voiceflow 自动充值说明里写着 `Nothing is charged in advance.`）。另补三条可用素材：Voiceflow 额度耗尽后的**四个出口**（通话被拒接 / 呼叫人听到「无可用 agent」/ builder 测试通话提示加额度 / 管理员收邮件）——正好对应我们提醒文案那三句要覆盖的**三类不同的人**；Bland 实时 TTS 按「已交付音频的块」计费、打断照收已交付部分、每 turn $0.001 最低消费，且**平台自认按字符计费只是块粒度估算**（计量粒度能写到什么程度的上限参考）；Bland 计费端点全量 grep 后确认**只有两个且都是 GET**。
- **本轮刻意不改的**（记进差距清单，不是缺陷）：「平台与并发服务费不进余额」在腾讯 / 阿里 / 火山 / 华为的公开资料里查不到对应写法（它们只有「套餐含分钟数」或「资源包」两种纯形态），**这是我们自创的口径、不是行业做法**——但它站得住（客户确实为并发容量付了钱，那部分钱不该变成能打电话的余额），只是文案里不要拿行业背书；导出审计（除华为云 CTS 外国内查不到）与每条流水自带变动后余额属领先项；万元级主动授信垫付与国内「延停 / 免停」的 10 元 / 10 天量级差异最大，保持不动。
- 本轮**未做**：自动充值不落库（设置只在页面状态里，保存不产生任何东西）、续费 / 加购 / 开票只到「提交申请」为止（付款后生效的链路是后端的事）、到期提醒没有真实的触发源（与提醒记录同样从空数组起步，`NOTIFY_RECORDS = []` 不变，不为演示编假记录）。
- **收尾：按反馈砍掉强制横向滚动条与多余说明文字**。反馈是「文字太多，没有这么做滚动条的，一点不现代」。查下来滚动条的根因是**13 处 `min-w-[680~1080px]`**：侧栏 276px，1440 屏下表格可用宽度约 1070px，`通话消费明细` 写死的 1080px 在常见屏宽下**必然**出现横向滚动条，1280 屏下连 900px 的表也会滚。已全部删除写死的 `min-w-[Npx]`，改为让表格按内容自然取宽——**保留 `overflow-x-auto` 但只在内容真的放不下时才出现**（这也正是 DESIGN.md 第 68–69 行原本就写的「主要面向桌面后台，窄屏横向滚动表格」）。
- **说明文字砍到 1–2 行**。15 处 `<Note>` 与十余处 `desc` 原文平均 4–6 行，原因、依据、行业对照全写在屏幕上了。**理由留在代码注释里（`//`），屏幕只留「客户要据此做什么」那一句**——这是这次改动的原则：解释是给维护者看的，客户只需要知道规矩是什么。典型如 `NotifyRecords` 的三段式 Note（约 400 字）压成两行、`RechargePanel` 自动充值的四条长说明压成四条短列。**未删的一句都没浪费**：所有被砍掉的理由都还在 `//` 注释或 `docs/计费中心竞品调研.md` 里，需要时能查回来。**核对过没有任何守卫断言挂在被砍的文案上**（`不会自动充值`、`各算各的`、`唯一支持代扣的方式`、`maxPerDay` 等被断言的字样全部保留）。
- **这一轮没有动的东西要说明白**：`NOTIFY_RECORDS` 仍是空数组，所以「提醒记录」仍是一张空表——**这些提醒现在一条都发不出去**（原型没有服务端、没有真实触发源），页面上的「提醒文案」只是「真发出去会是什么样」的预览。**页面上没有任何内容需要悬停才会出现**（全仓 `components/billing/` 里只有 1 处 `title=`，是 Call ID 的原生 tooltip，不是说明文字），所以说明文字是常驻的、不需要操作就能看到；唯一默认收起的是「充值」与「自动充值」两块表单（上一轮按「空间浪费」的反馈收的）。
